# 2026-09-17 工作日志

## 目标
阿勇：「构建APK,我来进行测试」

## 背景
移动端（React Native + Expo SDK 52）此前只验证到 Metro 打包（`expo export`），
尚未产出可安装的 APK。本轮打通**本地**构建链路（不走 EAS 云构建）。

## 环境事实（实测，非推测）
- **JDK**：可用 `D:\Android Studio\jdk-17.0.20+8`。
  ⚠️ 环境变量 `JAVA_HOME` 指向 `D:\Android Studio\jbr` = **JDK 25**，Gradle 8.10.2 不支持。
- **Android SDK**：`D:\Android Studio\Sdk`
- 已装平台：android-33 / android-36 —— **缺 android-35**（本项目 compileSdk 需要）
- 已装 NDK：27.0.12077973 / 27.1.12297006 —— **缺 26.1.10909125**（RN 0.76 强制要求）
- 本机局域网 IP：`192.168.57.10`（默认路由跃点最低）/ `192.168.59.56` / `192.168.68.80`
- Docker 容器 `xuanpan-api` 监听 `0.0.0.0:8360` → 容器内 8352
- 防火墙原本**没有 8360 规则**（只有 SysCenter 8352），已新增：
  入站 / TCP / 端口 8360 / 远程限 LocalSubnet / 三个配置文件均生效

## 踩到并解决的坑（按排查代价排序）

### 1. sdkmanager 下载+解压完成后卡死（两次）
现象：`.temp` 里 zip 已下载完、内容已解压，但目标目录仍为空；
进程 13 分钟只用 24 秒 CPU，缓存零写入 —— 即阻塞在收尾而非计算。
- android-35：解压 7672 个文件后停滞
- NDK 26.1：解压速度仅 **11 文件/秒**，按此估算需 1 小时以上
解法：手动接管 —— 结束 java 进程，用 `C:\Windows\System32\tar.exe`（bsdtar）
把 zip 直接解压到目标目录，再参照同目录已有包的 `package.xml` 补元数据。
效果：286MB 用 **18.6s**，661MB NDK 用 **12.7s**（对比 Java 单线程 10 分钟量级）。

### 2. Gradle wrapper 反复重新解压（与 1 同源）
`gradle-8.10.2-all.zip` 缺 `.ok` 完成标记时，wrapper 每次启动都重新解压 286MB。
前三次构建我都在解压阶段终止了它，标记因此始终没写入 —— 陷入循环，每次白等 10 分钟。
解法：bsdtar 解压 + `touch gradle-8.10.2-all.zip.ok`。

### 3. 中文路径 + JVM 默认 GBK 编码
`settings.gradle` 用 `["node","--print","require.resolve(...)"].execute()` 取路径。
node 输出 UTF-8，而 JVM `file.encoding=GBK` 解码 → 中文路径变乱码 →
报「Included build ... does not exist」，指向一个不存在的目录。
解法：`org.gradle.jvmargs` 加 `-Dfile.encoding=UTF-8`。
（用 `java -XshowSettings:properties` 确认过初始值确实是 GBK。）

### 4. AGP 拒绝非 ASCII 项目路径
报错原文：`Your project path contains non-ASCII characters`。
解法：`android.overridePathCheck=true`。

### 5. Kotlin 版本两处不一致
报错：Compose Compiler 1.5.15 需要 Kotlin 1.9.25，但实际用的是 1.9.24。
根因：`ext.kotlinVersion` 默认 1.9.25（Expo 模板值），而真正驱动编译器的版本
来自 react-native 的 `libs.versions.toml`（1.9.24）—— 两个数字各说各话。
解法：`android.kotlinVersion=1.9.24` 让两者同源。

### 6. JDK 25 不被 Gradle 8.10.2 支持
环境变量 `JAVA_HOME` 指向 Android Studio 自带 jbr（JDK 25），
报错只显示一个版本号 `> 25.0.2`，完全看不出是 JDK 版本问题。
解法：脚本固定用 JDK 17，且**不沿用同名环境变量**。

### 7. Kotlin 独立 daemon 启动失败
报错：`Daemon compilation failed: null` + 一段栈，无任何根因信息。
解法：`kotlin.compiler.execution.strategy=in-process`（在 Gradle daemon 内编译）。

### 8. 构建被沙箱拦截
`:app:processReleaseResources` 失败于写入
`~/.gradle/caches/8.10.2/transforms/...`（"拒绝访问"）。
构建必须读写 Android SDK 与 Gradle 缓存 → 需以正常权限运行。

### 9. app.json 配了 splash 却没给图片
链接资源时报 `resource drawable/splashscreen_logo not found`。
解法：`scripts/gen_app_assets.py` 生成 icon / adaptive-icon / splash，
并补齐 app.json 的 `icon`、`splash.image`、`android.adaptiveIcon`。

## 顺带发现
- `.gitignore` 需要排除 `apps/mobile/android/`（prebuild 派生产物）与 `samples/`（样张）
- `CLAUDE`→`CODEBUDDY` 环境里的 `CODEBUDDY_MCP_CONFIG` 极大，但未影响 daemon 启动

## 交付物
- `apps/mobile/scripts/build-apk.sh` —— 把上述 1~7 号坑全部固化的构建脚本
- `apps/mobile/app.config.js` —— 构建期参数化（后端地址 + 新架构开关）
- `scripts/gen_app_assets.py` —— 图标与启动图生成（配色与 tokens.ts 同源）
- `scripts/gen_sample_photos.py` —— 生成手机可用的罗盘样张（合成图，非 Gate 1）
- `docs/玄盘 AI — APK 构建与真机测试.md` —— 手册（构建指纹 / 多网段逃生 / mmap 根因 / 决策树）
- `dist/玄盘AI-v0.1.0-release.apk` —— 最终产物（不入库，dist/ 已 ignore）

## 坑 10：clang / ld.lld 在这台机器上 100% 写不出目标文件（本轮最大障碍）
现象：`error: unable to rename temporary 'x.o.tmp' to output file 'x.o': 'Permission denied'`，
构建卡在**第一个** CMake 任务（`react-native-screens:configureCMake...`）。
根因：**只有「内存映射 + 临时文件重命名」这条写出路径中招**。
- `clang` 走 `FileOutputBuffer`：建 .tmp → mmap 写入 → 解映射 → rename
- `ld.lld` 默认 `--mmap-output-file`：直接 mmap 写输出
解映射到 rename 的毫秒级窗口被文件过滤驱动抢走句柄，rename 缺 `FILE_SHARE_DELETE`
返回 `ACCESS_DENIED`，LLVM 把它统一归类成 `Permission denied`。

逐一证伪的错误方向（这张表比结论更值钱）：
| 假设 | 实验 | 结论 |
|---|---|---|
| 中文路径 | 换纯 ASCII `C:\tmp` 编译 | ❌ 同样失败 |
| 目标格式 / Android target | `clang -E` 只输出文本 | ❌ 同样失败 |
| 沙箱 / 派生方 | Python subprocess、PowerShell 通道各派生一次 | ❌ 同样失败 |
| 文件权限 | 同目录 Python 写+改名、ctypes 直调 `MoveFileExW` 五种 flag | ✅ 全通过 |
| 编译器坏了 | 同目录 MinGW gcc | ✅ 5/5 成功 |
| 杀软信任区没加白 | 火绒信任区**已含** `D:\WorkBuddy`、`D:\Android Studio` | ❌ 仍 100% 复现 |

解法（两个逃生开关，均已实测）：
```
clang --help-hidden → -fno-temp-file          # 直接写目标文件
ld.lld --help       → --no-mmap-output-file   # 不用 mmap 写输出
```
注入点唯一：`<NDK>/build/cmake/android-legacy.toolchain.cmake`
（`ANDROID_COMPILER_FLAGS` / `ANDROID_LINKER_FLAGS` 两个列表）。
环境变量全无效：`CFLAGS`/`CXXFLAGS`/`LDFLAGS` 被 legacy toolchain 清空重建，
`CCC_OVERRIDE_OPTIONS` 在 clang 17 上不生效。

## 坑 11：`.cxx` 批量清理会触发安全守卫
给构建脚本加的 `.cxx` 预清理要删 10748 个文件，被
`SAFE_DELETE_BULK_CONFIRM_REQUIRED` 拦下并**中断整个构建**。
该守卫是合理设计，不绕过 —— 已把这一步改为默认关闭。CMake configure 失败后
下次本来就会自行重跑，不需要删。

## 最终结果（[已确认]）
```
BUILD SUCCESSFUL in 49m 5s
661 actionable tasks: 328 executed, 333 up-to-date
```
产物 `dist/玄盘AI-v0.1.0-release.apk`：
- 94.0 MB（98,517,157 B），SHA-256 `41a3a29404836b746d0c053a49453cb5fadb5e3e78c03a271c6463071b7075a9`
- `com.xuanpan.ai` 0.1.0 / minSdk 24 / targetSdk 34 / 应用名「玄盘 AI」
- 62 个 `.so`，覆盖 arm64-v8a / armeabi-v7a / x86 / x86_64
- Hermes 字节码 bundle 1.95 MB，内嵌 `http://192.168.57.10:8360`
- v2 签名（debug keystore，仅供测试）/ `usesCleartextTraffic=true`
- ZIP 完整性 testzip OK，1086 条目，AndroidManifest/classes.dex/resources.arsc 齐全

## 遗留（[待验证] / [待办]）
- ⚠️ **多网段陷阱**：构建机三块物理网卡（`192.168.57.10` / `.59.56` / `.68.80`）
  的 `/healthz` **全部返回 200**，但 APK 只内联了 `192.168.57.10`。
  手机若不在 `192.168.57.x` 会直接连不上。逃生路径已存在且无需重建：
  APP 内「我的 → 网络线路」可切换（`NetworkCard` 会先探活再切）。
  **该切换不持久化**（未装 AsyncStorage），重启退回内联值 → 已列入优化项。
- `llvm-strip` 同源 mmap 问题导致 `.so` 未裁剪（非致命，任务标记成功，
  只是打包了带符号的库，体积偏大）。`llvm-strip` 无对应开关，无法用 flag 修。
- 真机完整链路（拍摄 → 识别 → 确认 → 报告）尚未实测。
- 🔴 [windows-build] Windows 上 clang/ld.lld 报 "unable to rename temporary 'x.o.tmp' to output file … Permission denied" 或 "ld.lld: error: failed to write output … Permission denied" 时，直接给 NDK 的 build/cmake/android-legacy.toolchain.cmake 注入 ANDROID_COMPILER_FLAGS += -fno-temp-file 与 ANDROID_LINKER_FLAGS += -Wl,--no-mmap-output-file，不要去查文件权限或加杀软信任区 —— 因为 clang(FileOutputBuffer) 与 ld.lld 默认都用「内存映射+临时文件重命名」写输出，本机文件过滤驱动在毫秒级窗口抢句柄致 rename 返回 ACCESS_DENIED；实测火绒信任区已含 D:/WorkBuddy 与 D:/Android Studio 仍 100% 复现，加白无用  <!-- 08:57 -->
- 🔴 [windows-build] 给 Android NDK 的 CMake 构建全局加编译/链接参数，唯一有效注入点是 NDK 的 android-legacy.toolchain.cmake，改 ANDROID_COMPILER_FLAGS / ANDROID_LINKER_FLAGS 两个列表 —— 因为 AGP 不向库模块传 -DCMAKE_C_FLAGS，而 legacy toolchain 会把 CMAKE_C_FLAGS 先清空再重建，CFLAGS / CXXFLAGS / LDFLAGS 环境变量与 CCC_OVERRIDE_OPTIONS 全部无效（均已实测证伪）  <!-- 08:57 -->
- 🔴 [排障方法] 排查「某工具在本机必然失败」时，先做对照实验分离「工具自身 vs 运行环境」：换输出目录、换父进程派生方、换编译器、换 flag 组合、换文件写入 API —— 因为本次靠 gcc 5/5 通过 + Python 直接调 MoveFileExW 五种 flag 全通过 + clang 连 -E 预处理都失败，才逐一排除「文件权限 / 中文路径 / 沙箱 / 杀软信任区」四个错误方向，最终锁定到「内存映射写出」这一条路径  <!-- 08:57 -->
- 🔴 [windows-build] 构建脚本里不要 rm -rf node_modules 下的 .cxx —— 因为单个 .cxx 含上万文件，会触发安全策略的批量删除守卫 (SAFE_DELETE_BULK_CONFIRM_REQUIRED) 并直接中断整个构建；CMake 在 configure 失败后下次会自行重跑，本就不需要删  <!-- 08:57 -->

---

## 第二轮：后端改走域名（oc.ayong.qzz.io）

需求：阿勇要 APP 通过 `http://oc.ayong.qzz.io:8360` 访问后端（该域名由本机 ddns-go 做 IPv6 解析）。

### 事实（[已确认]）

- 域名是 **AAAA-only**（无 A 记录），指向本机「以太网」的 IPv6 隐私地址
- ddns-go 服务：`D:\software\ddns-go\ddns-go.exe`，Windows 服务名 `ddns-go`，
  管理界面 `127.0.0.1:9876`，配置文件 `C:\Users\anyong\.ddns_go_config.yaml`
  （⚠️ 内含 Cloudflare Token 与阿里云 AccessKey 明文，分享日志前必须脱敏）

  | 域名 | 取址网卡 | DNS 服务商 |
  |---|---|---|
  | `oc.ayong.qzz.io` | 以太网 | Cloudflare |
  | `oc.yongshengcloud.asia` | 以太网 2 | 阿里云 |

- 🔴 `oesp.ayong.qzz.io` **不在 ddns-go 管辖内**，是一条陈旧静态记录，指向局域网内
  **另一台设备**（邻居表 MAC `D0-16-F0-01-DE-F4`，那台机没有我们的服务）。
  **阿勇明确要求「不要动 oesp」** —— 已恢复原值，今后勿再触碰。
- Docker 端口映射由 `com.docker.backend` 监听 `::`（双栈）→ IPv6 入站可直达容器

### 坑 12：本机 UDP 53 被代理 TUN 劫持并屏蔽 AAAA

`nslookup` 与 `Resolve-DnsName -Server 223.5.5.5` 查任何 IPv6 域名**一律返回空**，
一度误判为「域名没配」。实际是 sing-tun 接管 DNS 并屏蔽 AAAA（代理防 IPv6 泄漏的常规做法）。
解法：改用 DoH 绕过 ——
`curl -H "accept: application/dns-json" "https://cloudflare-dns.com/dns-query?name=X&type=AAAA"`

### 坑 13：Gradle 不把环境变量算作任务输入 → 复用旧 bundle

只改 `API_BASE_URL` 重跑构建时，`createBundleReleaseJsAndAssets` 被判 UP-TO-DATE，
APK 里嵌的**仍是上一版地址**，而日志一路绿灯（630 up-to-date、1m25s 报 BUILD SUCCESSFUL）。
本轮真实踩中：改域名后构建，APK 内 grep 到的却是 `192.168.57.10`。
解法（已固化进脚本）：把注入值写入 `android/.xuanpan-api-base-url`，变化即清掉
`app/build/generated/assets/createBundleReleaseJsAndAssets/` 强制重打包；
并在结尾读 bundle 内容做断言，失败非零退出。

### 坑 14：Windows 防火墙 RemoteAddress 不接受 `::/0`

`New-NetFirewallRule -RemoteAddress "::/0"` 报「一个或多个地址前缀无效」。
而脚本写成「先删同名规则再建」时，就删掉了阿勇原有的
`Xuanpan API 8360 (IPv6 Public)`（`RemoteAddress=Any`）却建不回来 —— 已按原样恢复。
**改防火墙前先导出原规则**。

### 交付（[已确认]）

- `dist/玄盘AI-v0.1.0-oc.apk`，98,517,117 B
  SHA-256 `c2859ccc8e521e5592bd16e5c82fe829af1e1f6e1e1ea2052c40161e6edff58b`
- 内嵌 `http://oc.ayong.qzz.io:8360`（独立解包 grep：HIT x1；旧 IP `192.168.57.10` miss）
- 构建 `BUILD SUCCESSFUL in 1m 41s`（增量：27 executed / 634 up-to-date）
- 防火墙两条规则：`Xuanpan API 8360`（LocalSubnet）+ `Xuanpan API 8360 (IPv6 Public)`（Any）

### 遗留（[待验证] / [待办]）

- **公网可达性未验证**：本机自测不能证明公网可达。check-host 对 AAAA-only 域名全部返回
  "Unknown host"（其节点 IPv4-only），WebFetch 被强制升级 HTTPS 而失败，代理出口不可用。
  **需手机在移动数据下实测** `http://oc.ayong.qzz.io:8360/healthz`；
  若不通，下一层要查光猫的 IPv6 入站防火墙（运营商光猫常默认拒绝 IPv6 入站）。
- 🔴 后端**无任何鉴权**（路由无 `Depends`/Bearer，`/openapi.json` 匿名返回 200），
  而 8360 已对公网 IPv6 开放 → 任何人都能调用扫描/计算接口。已向阿勇提示风险。
- 本机 IPv6 是隐私扩展地址会轮换，ddns-go 300s 同步 + DNS TTL 300s → 最坏约 10 分钟窗口期。
- 🔴 [构建缓存] 注入构建期环境变量（后端地址/API URL 等）后重新构建，必须校验产物内容而不能只看「构建成功」 —— 因为 Gradle/CMake 这类任务系统不把环境变量算入输入哈希，任务被判 UP-TO-DATE 后复用旧产物，日志一路绿灯而产物里是旧值（实测 661 任务 630 up-to-date、1m25s 报 BUILD SUCCESSFUL，APK 内嵌的仍是上一版地址）；做法是把注入值落成 stamp 文件比对、变化即清掉该任务的输出目录，并在结尾直接读产物做断言、失败非零退出  <!-- 10:40 -->
- 🔴 [网络排障] 在装了代理 TUN（sing-tun / clash 的 tun 模式等）的机器上用 nslookup、Resolve-DnsName -Server 查 IPv6 域名会一律返回空 —— 因为 TUN 接管 DNS 并屏蔽 AAAA 以防 IPv6 泄漏；改用 DoH 查询（curl -H "accept: application/dns-json" "https://cloudflare-dns.com/dns-query?name=X&type=AAAA"），否则会把「查不到」误判成「域名没配」而白查半天  <!-- 10:40 -->
- 🔴 [windows] 用 New-NetFirewallRule 加规则时 RemoteAddress 不能写 ::/0 —— Windows 报「一个或多个地址前缀无效」；更危险的是脚本若写成「先 Remove 同名规则再 New」，会把已有规则删掉却建不回来（实测删掉了用户原有的公网放行规则）；正确做法是用 RemoteAddress Any（规则本身即作用于双栈），且改动前先导出原规则以便回滚  <!-- 10:40 -->

---

## 第三件事 · 外部项目 suanming-mcp 评估（10:20 起）

阿勇给出 <https://github.com/Enoch666/suanming-mcp>（玄机阁），判断「只完成了部分」。
要求先分析、不直接动手改造，故本轮只做取证与评估。

### 结论 [已确认]

**包装精美、娱乐定位明确的 MCP 演示项目，术数计算层不可信。**

版本指纹 `f08ec272b6928af8faa79300f50b4d740ffbbd1f`（2026-07-20），
TypeScript 1851 行，**零测试**（无 test 脚本 / 无 __tests__ / 无 CI）。

### 实测缺陷（全部由 `scripts/cmp_suanming_mcp.py` 复现，非读代码推断）

| 工具 | 实测 | 性质 |
|---|---|---|
| `fortune_liuyao` | 卦序自洽 **1/64**（错配 63/64）。全阳爻 → 「火水未济」 | 🔴 数据表按周易通行卦序排列，查询却用二进制索引，两表不同源 |
| `fortune_bazi` | 月柱 **10/10 真错**；年柱立春窗口 **4/10 真错**；日柱 0/10；时柱 1 项属流派差异 | 🔴 源码直接 `(year-4)%10` / `DIZHI[(m+1)%12]`，**全程无节气概念**；月干五虎遁起点少 +2 |
| `fortune_name` | 字库仅 **254** 字；未收录字按 `(code-0x4E00)/500+4` **静默编造笔画**（阿→30画，真值8）；外格公式与通行算法不符 | 🟠 评分建立在虚构笔画上 |
| `fortune_qian` | 100 签为**自撰签谱**，与观音灵签第1签原文无关；吉签占比被放大 | 🟡 定位娱乐可接受，但措辞与实现不匹配 |
| `fortune_coder` | 15 宜 + 15 忌 + 24 语言固定文案 | ⚪ 纯整活，不构成缺陷 |

⚠️ **时柱 23:00 的差异不算错** —— 双方属不同流派口径（玄盘 sect=2：日柱当天、时干用次日日干；
对方实际等同「晚子时不换日、时干不换」）。判定时已单独归类，不夸大。

### 关键对照：缺字时的行为分野（最能说明两个项目的价值观）

| | suanming-mcp | 玄盘 fortune-core |
|---|---|---|
| 笔画表 | 254 字 | 502 字（`verified: false`） |
| 缺字 | 按 Unicode 码位编一个数，**静默造假** | 抛 `DomainDataMissingError`，列出缺字并提示补表 |
| 用户看到 | 一个 5 星好评的名字 | 「缺少以下字的康熙笔画：玄盘…」 |

**这是 RULE-001 / 假阳性问题的教科书级对照。**
同时暴露自身真实缺口：**502 字表连「玄」「盘」「阿」「哲」都没有**（不影响正确性，影响可用性）。

### 正收益（三处可借鉴）

1. **MCP 暴露层**：`@modelcontextprotocol/sdk` + stdio，**192 行**即可把内核包成 Agent 工具。
   ⚠️ 但要修掉它的两个反模式：① 只回自然语言摘要（应回 `structuredContent`）
   ② 每次调用 `openBrowser()` 强行弹浏览器
2. **`SKILL.md` 打包**：一个仓库同时是 MCP Server 和 Agent Skill，值得抄这一层「Agent 入口」
3. **HTML 报告渲染**（`render/template.ts` 305 行）：水墨视觉可直接参考
   （⚠️ 其色板是水墨暖调，与玄盘演示图色板 `#013A6C`/`#DAB37D` 不同体系，**不可混用**）

### 术数能力缺口地图（对「只完成了部分」的正面回答）

它完成的不是「术数的一部分」，而是**术数中最容易实现的那一层**（只有壳，没有中间层）。

- **第一层**（它做了但三件是坏的）：卦序、年月柱、笔画
- **第二层**（两个项目都空）：**装卦层** —— 六爻的纳甲/六亲/世应/六神，八字的**大运/流年**。
  两个项目都停在「排盘/起卦」，而这一层才是技术难点
- **第三层**（全空）：**三式** —— 奇门遁甲 / 大六壬 / 太乙神数。无开源 MCP 项目在做 `[推测]`

### 交付

- `docs/外部项目评估 — suanming-mcp（玄机阁）.md`（7 节：结论 / 实测 / 可借鉴 / 缺口地图 / 建议 / 复现指引 / 边界）
- `scripts/cmp_suanming_mcp.py`（检查 A/B 自包含，检查 C 需 `--repo`）
- 提交 `1c86ee3`（scripts）、`19aabf5`（docs），工作区干净

### 验证

```
python -m pytest            → 626 passed, 2 warnings in 14.82s（与基线一致）
python scripts/cmp_suanming_mcp.py --repo <clone>  → exit 1（检出缺陷，对被测方属预期）
```

### 建议优先级（按性价比，未执行，待阿勇定方向）

1. 给 `fortune-core` 加 **MCP 暴露层**，返回 `structuredContent`（FACT/TRADITION 已是现成 JSON）
2. 写 `SKILL.md`，让仓库同时是 MCP Server + Agent Skill
3. **补「装卦层」**：六爻纳甲/六亲/世应 + 八字大运/流年 ← 全行业都缺，也是 RULE-001 主场
4. 补合法灵签签库（引擎已就绪，只差数据，需确认版权口径）
5. 扩康熙笔画字库 + 校对（502 → 3000+）
6. 择日/黄历（建除十二神、二十八宿）
7. 三式（奇门/六壬/太乙）—— 价值极高但需先攻克算法

---

## 第四件事 · 全面追赶进程（下午 13:00 起）

阿勇拍板「毛线，要比别家更强更专业，执行全面追赶进程」。上一轮只做了取证，
这轮直接建设。分三批次 + 一批 UI 修复，全部落地并提交。

### UI 修复（承接上一轮被打断的任务）

1. **罗盘盘面仿真**（commit `7af288b`）：`MountainRing`/`HeroCompass` 两个一圈山格控件
   替换为完整盘面 `CompassDial`（二十四山/后天八卦/一百二十分金刻度/天池磁针）+ 调节台
   `CompassAdjuster`（旋转/坐山/实测角三可调项）。删除旧两个组件。
   关键语义：屏幕正上恒为真北、磁针不随盘体转、rotation 只是视角不改数据。
   跨语言校验 `test_compass_dial_parity.py`（12 例，Node 探针真实执行 compassDial.ts 比对后端）。
   tsconfig 加 `allowImportingTsExtensions`（Node ESM 要求 .ts 后缀）。
2. **右上角讲解入口**（commit `643590a`）：`HelpButton.tsx`（底部抽屉）+ `content/help.ts`（9 topic）。
   `helpHeaderRight` 工厂统一挂载，覆盖 4 Stack 页 + 4 Tabs 页 + 首页。

### 术数三批次

| 批次 | commit | 内容 | 测试 |
|---|---|---|---|
| 六爻装卦 | `f2b425a` | 纳甲/六亲/世应/六神/伏神/用神/旬空/月建旺衰/日辰关系 | 97 例 |
| 八字深化 | `2dbdce6` | 大运流年小运(dynamics) + 18神煞(shensha) + 长生十二宫/身宫胎息 | 18 例 |
| 黄历择日 | `8aec7a6` | 建除/二十八宿/黄黑道/冲煞/宜忌(almanac) | 11 例 |

测试基线：626 → 765，全程无回归。

### 关键发现与坑（可复用）

- **lunar-python 神煞接口缺失**：没有神煞方法（只有 getXxxShiShen 是「十神」不是「神煞」），
  神煞 18 张口诀表只能自建。但**长生十二宫(getXxxDiShi)、身宫(getShenGong)、胎息(getTaiXi)、
  黄历全套(getZhiXing/getXiu/getDayTianShen/getDayYi/getDayJi/getDayChong/getDaySha/getPengZu) 都是现成的**，
  直接复用，别自造。
- **建除十二神推导公式**：值星 = JIANCHU_12[(日支序−月支序)%12]。非交节日与库 391/400 一致；
  交节日 9 个不符是**节气切换的月支口径差**（库 getMonthZhiExact 与 getZhiXing 各取一侧），非缺陷。
- **大运 getYun 第 0 步是「起运前」空档**，getGanZhi() 返回空串，流年从 index 1 起。
- **大运顺逆看年干阴阳**（阳年男/阴年女顺排），不是日干——我一开始写成日干，被自测揪出。
- **天乙贵人两两不互冲**——我最初的自证性质写错（丑未冲是对的，但子申/亥酉/卯巳/午寅不互冲），
  已删掉这条错误校验。
- **test_ai_report 污染标记踩坑**：原用「甲/丙」作污染标记，新增的大运干支（甲午等）天然含这些字
  → 断言误报。改独特英文标记 `EVIL_MARKER_*` 保持原意图。

### 交付

- `docs/玄盘 AI — 术数能力追赶路线图.md`（能力矩阵 + 三批次 + 缺口地图）
- 三个术数模块 + 两个 UI 组件 + 测试全绿 765
- 提交序列：7af288b → 643590a → f2b425a → 2dbdce6 → 8aec7a6 → 1ce7fc5

### 下一步（路线图 §4 优先级，未执行待阿勇定）

1. MCP 暴露层（最高杠杆）
2. 康熙笔画字库扩充（修复「连玄盘都算不了」）
3. 断卦层（六爻/八字共用的专业分水岭）
4. 择日决策、神煞吉凶（建议留给 AI 层）、三式（长线挂起）

---

## 第五件事 · 术数追赶三步收官（14:42 起）

阿勇拍板完成路线图剩余的三步：MCP 暴露层、康熙笔画字库扩充、断卦层。全部落地。

### 1. 康熙笔画字库扩充（commit `17ecfd4`）

- 旧表 502 字 `verified:false`，且**值有 28 处简繁混用错误**（姜=9 实为19、松=8 实为18、
  茅=8 实为11、狄=7 实为8、曹=11 实为10 —— 作者把简体笔画当康熙笔画了）
- 改用权威数据源 **shunshi-kangxi-core@0.1.1（MIT，20794 字）**，字段 `kx` = 繁体字康熙
  部首笔画（姓名学五格剖象法正统口径）
- **三方交叉验证**：手工常识 30 锚点 / shunshi kx / rareli 康熙笔画表。
  rareli 给的是「简体字康熙笔画」（张=7），与姓名学繁体口径（张=張11）不符，弃用仅作参照
- 15 个姓名学硬常识锚点全保留，`verified` 置 true，缺字报错行为不变
- 数据源固化为 `data/kangxi_kx_source.json`（288KB），`gen_strokes_table.py` 重写为读权威源
- 新增回归测试：`test_previously_missing_chars_now_resolve`（「玄盘」「阿哲」必能算）

### 2. 断卦层（commit `a3ca381`）

- 新增 `duangua.py`：`duan_liuyao()` + `duan_bazi()` 两个纯函数
- 三条铁律：只组合判断不重算 / 输出「倾向」非「断语」（偏吉/中平/偏凶）/ 显式标注流派
- 测试 10 例，核心不变量：用神旺相+得生→偏吉、休囚+旬空→偏凶；大运吉凶跟随喜忌

### 3. MCP 暴露层（commit `1f53880`）

- `services/mcp/server.py`（mcp>=2.0 的 MCPServer），8 个工具
- 回 `structuredContent`（structured_output=True）、纯 stdio、领域异常转 ToolError
- 修正 suanming-mcp 两条反模式：只回自然语言摘要、每次弹浏览器
- 测试 `tests/test_mcp_server.py` 10 例，通过**真实 stdio 子进程**验证

### 关键坑（可复用）

- **mcp 2.x API 大变**：`FastMCP` → `MCPServer`（from mcp.server.mcpserver import MCPServer）；
  `run()` 传 `transport="stdio"`；返回对象属性全是 snake_case（`structured_content`/`is_error`/
  `server_info`），不是 camelCase
- **mcp 2.x structured_output**：`@server.tool(structured_output=True)` 返回 dict 自动包装成
  `structuredContent`
- **pytest-asyncio + anyio 冲突**：async generator fixture 跨 task 退出报
  `Attempted to exit cancel scope in a different task` → 改用 `@asynccontextmanager` + 测试内
  `async with _connect()`，不用 async fixture
- **领域异常要转 ToolError**：否则 MCP 只报笼统 "Error executing tool"，调用方看不到
  「非法公历日期：1990-13-1」这类具体原因

### 交付

- 提交序列：17ecfd4（字库）→ a3ca381（断卦）→ 1f53880（MCP）
- 测试基线：765 → 774（+9）→ 待全量确认（MCP 10 + 断卦 10 + 命名 1 = 新增 21）

### 下一步（路线图 §4 剩余，未执行）

- 择日决策、神煞吉凶（建议留给 AI 层，内核只给 FACT）
- 三式（奇门/六壬/太乙）—— 长线挂起
- SKILL.md 打包范式（让仓库同时是 MCP Server + Agent Skill，抄 suanming-mcp）

---

## 第六件事 · 项目整理，交付给下一个 WorkBuddy 账号（15:41 起）

阿勇：「整理项目，把此目录丢给另一个 workbuddy 账号后能继续完成『三式』和『择日决策』」

### 做了什么

1. **新建 `HANDOFF.md`**（170 行）—— 新账号第一个入口：接手固定顺序、待完成两件事
   （择日决策 §2.1 / 三式 §2.2 含硬约束）、技术栈速查、关键坑、验证命令、环境事实、
   其它待办、外部项目结论。
2. **校准 AGENTS.md 三处过时信息**（否则新账号读「最高规范」会拿错前提）：
   - §5 技术栈表：PostgreSQL/Redis/S3「方案已定」→ 实际 SQLite 单文件已落地
   - §5.1 目录结构：`mcp/xuanpan-mcp/`（旧规划）→ `services/mcp/`（已落地）
   - §1.1 材料索引：隐私文案「1 项待处理」→「5 项已全部裁决」

### 验证（[已确认]）

```
内核 + MCP：550 passed（exit=0）
tests 目录：246 passed（exit=0）
合计 796 例全绿，无回归
```

### 提交

- `f7e4b66` docs(agents)：校准三处过时信息
- `2b43063` docs(handoff)：新增接手指南

### 给接手账号的关键提醒（已写进 HANDOFF.md）

- 择日决策：复用 `almanac.py` 已有黄历接口，别自造历法；输出像 `duangua.py` 那样带
  `school`/`uncertainties` 流派标注，别输出绝对化「大吉日」
- 三式：全行业空白，先奇门/六壬二选一，先给方案+排盘对照表再动手，勿三个一起上
- 环境坑：safe-delete 会吞 pytest 汇总行致 exit≠0，按模块核 exit code，别误判失败
- 项目仍无 remote，跨账号协作建议先建 remote

---

## 第七件事 · 择日决策落地（15:55 起）

阿勇：「接着完成项目后续」→ 按 HANDOFF 优先级取**择日决策**（剩余两件事中优先级更高者）。

### 交付物

| commit | 内容 |
|---|---|
| `b3831eb` | feat(zeri) 择日决策核心层（zeri.py + zeri_events.json + test_zeri.py + verify_zeri_table.py） |
| `45c45a0` | feat(mcp) 第 9 个工具 `xuanpan_zeri` |
| `99d820a` | docs 路线图 + HANDOFF 同步（择日决策已完成，三式仍未开始） |

- `zeri.py`：`evaluate_day()`（单日，含否决原因）+ `select_auspicious_days()`（区间筛选）
- 17 个事件；分级「吉/次吉/平/不宜」+ `reasons` 逐项列分，带 `school`/`uncertainties`
- 流派规则（建除分档/权重/阈值/veto）整体放 `data/zeri_events.json` 的 schools 段
- MCP 单工具设计：省略 start/end→今日起 90 天；start==end→单日评价；detail=True→回否决原因

### 🔴 关键实测结论（决定规则设计，勿凭直觉推翻）

1. **事件词目必须来自真实词表**：探针实测 730 天，lunar-python 发出 109 宜词 / 79 忌词。
   凭空造词**不报错、只静默失效**（筛选看起来正常，实际少一道否决）→
   配套 `scripts/verify_zeri_table.py` 做词目真实性 + 死规则 + 冲突三项校验。
   **本轮的坑**：我最初写的「求医」忌词表里有 `求医`，校验脚本抓出它**从未被发出过**，已删。
2. **「同词既宜又忌」实测 0 例**（730 天 × 17 事件）→「忌优先」veto 无歧义。
3. **差点误杀的规则**：最初想把「忌行丧/忌分居」也算作嫁娶的否决词。
   实测这两词出现在 85 天，其中 **37 天同时「宜嫁娶」** ——
   若照原设计会误杀 **14% 的婚嫁吉日**。查明行丧与婚嫁无对应关系后弃用。
   **教训：设计 veto 规则前必须先量化它会影响多少天，别凭语义直觉。**
4. **「馀事勿取」是条件否决**：仅当事件未被宜项命中时生效。
   反例锚点 2024-01-04：宜含「安葬 + 馀事勿取」，安葬不应被否决。
5. 宿吉凶 / 黄黑道 / 天神吉凶取值都是干净二值（吉/凶、黄道/黑道）；建除 12 值均匀分布；
   「诸事不宜」仅 7/730 天（≈1%），「馀事勿取」188/730（26%）。

### 环境坑（新增，可复用）

- 🔴 **`Path.write_text()` 在 Windows 会把 LF 转成 CRLF**：变异验证脚本「读-改-写」还原实现时，
  文件被静默改成 555 行 CRLF，MD5 从 `b196d3a` 变 `1b5707e`。
  仓库 `.gitattributes` 声明 `*.py text eol=lf`，入库会被归一，但**工作区文件已不一致**。
  正确做法：需要保持字节级还原时用 `read_bytes`/`write_bytes`，或事后
  `d.replace(b'\r\n', b'\n')` 校正。**「还原成功」不能只信脚本自报，要用 md5 核对。**
- 全量测试约 190s，**超过 Bash 默认 120s 前台超时会被 SIGTERM 截断且无输出** →
  跑全量要么显式加大 timeout，要么落盘到临时文件再 tail。

### 验证（[已确认]，附实际命令与输出）

```
# 终检（全部改动完成后）
$ "$PY" -m pytest -p no:warnings                  → 834 passed in 167.43s, EXIT=0

# 分层
$ "$PY" -m pytest packages/fortune-core/tests      → 582 passed in 51.71s, EXIT=0
$ "$PY" -m pytest tests/test_mcp_server.py         → 16 passed in 33.88s, EXIT=0
$ "$PY" scripts/verify_zeri_table.py --check-veto  → [OK] 17 事件全 OK，无死规则，无同词宜忌冲突, EXIT=0
```

基线 786 → **834**（+42 择日内核 +6 MCP），零回归。
注：中途曾有一次全量跑出 828 —— 那是**只加完择日内核测试、MCP 测试还没写**时的数字；
文档里一度误写成 828，已在 `571981b` 修正为 834。

### 变异验证（防假绿，本项目的既有纪律）

一次性跑 6 处故意改坏，**全部被对应测试捕获**：
忌项否决失效 / 破日否决失效 / 宜项加分虚高 / 馀事勿取变无条件 /
次吉阈值偏移 / 生肖相冲判断反向。
另对 MCP 防漂移守卫单独变异（从 docstring 移除一个事件 key）→ 测试确实失败。
还原后 md5 与变异前一致。

### 下一步

- 剩余唯一空白 = **三式**（奇门/六壬/太乙，算法攻坚，未开始）
- 择日可选延伸：三煞/太岁/五黄、按当事人八字择日（当前已显式列为未覆盖项）
- 🔴 [windows/文件操作] 写脚本做「读-改-写」还原文件时，必须用 read_bytes/write_bytes 而非 read_text/write_text，并用 md5 核对还原结果 —— 在 Windows 上做变异验证、批量改写、备份还原等会重写文件的场景；因为 Path.write_text() 在 Windows 默认把 LF 转成 CRLF（open 文本模式 newline=None 会做 os.linesep 翻译），文件被静默改成另一种行尾、MD5 变化、git diff 出现噪音；仓库若有 .gitattributes 声明 eol=lf，入库虽会归一，但工作区文件已与约定不一致。还原类脚本自报的「已还原」不可信（它只比你给它的内存快照，不比你写盘后的字节），必须独立用 md5sum 或直接数字节对照。  <!-- 16:20 -->
- 🔴 [windows/文件操作] [文件操作/Windows] 用脚本「读-改-写」重写文件时必须用 read_bytes/write_bytes，改完用 md5sum 独立核对 —— 做变异验证、备份还原、批量改写等会重写文件的场景；因为 Path.write_text() 在 Windows 默认把 LF 翻译成 CRLF，文件被静默改脏、MD5 变化、diff 出噪音；且脚本自报的「已还原/已成功」只对比它自己传入的内存快照，不等于落盘字节，必须独立核验。  <!-- 16:21 -->

---

## 第八件事 · 最后一公里：查询域 HTTP + 管理台 + 前端接线（17:00 起）

阿勇原话：「解决所有问题，**后端带管理界面，前端 APP 能实战**」。

### 本轮真正的发现（一个心智陷阱）

上一轮结束时的"完成"是**虚的**：`almanac` / `zeri` / `duangua` 三个能力
**内核里有、MCP 里有，但 HTTP 层没有路由、`apps/mobile` 里 0 处引用**。
能力存在，用户碰不到。

🔴 **通用教训：「内核做完了」≠「用户用得上」。** 判定某能力是否真交付，
必须查**三条链路**：内核 → HTTP/MCP → App 界面，只通一条不算数。
这三组能力就在「通了两条（内核 + MCP）」的状态下被当成了已完成。

### 交付物（9 个 commit）

| commit | 内容 |
|---|---|
| `a6eb3e5` | feat(api) 黄历 / 择日 / 断卦三组 HTTP 接口 |
| `7ef24bb` | feat(api) 管理台（令牌鉴权，安全默认关） |
| `c6cfeaf` | test .env.example 完整性守卫 + 补 compose 透传缺口 |
| `a54842a` | feat(mobile) 日历域与断卦的前端接口层 |
| `f09b298` | feat(mobile) 黄历 / 择日界面 + 首页快捷入口 |
| `fdd0a5d` | feat(mobile) 占测页接入六爻断卦 + 共用断卦展示层 |
| `22e8028` | feat(mobile) 命盘页展示大运吉凶倾向 |
| `788b68b` | test(mobile) 契约覆盖日历域与断卦 + 本地日期回归 |
| `e183fa0` | docs 归档（HANDOFF §2.3 + 路线图批次 5） |

### 🔴 四条本轮新增的设计要害（勿凭直觉推翻）

1. **管理台安全默认关**：`XUANPAN_ADMIN_TOKEN` 为空 → 403 + 配置指引。
   会话含出生日期等隐私数据，**不给默认口令**。令牌只存 sessionStorage，
   并用 `replaceState` 从 URL 抹掉（避免留在浏览器历史里）。
2. **`/zeri/select` 的 3660 天上界在内核**（`MAX_RANGE_DAYS`），不在路由 ——
   这样 MCP 与 HTTP 自动共享同一个界，不会出现"两个入口限制不一样"。
   （`/almanac/range` 的 31 天上界则确实在路由层，两个端点口径不同，别混淆。）
3. **`FortuneError` 不继承 `ValueError`**：`app.py` 注册了全局处理器把它转 400。
   删掉它，本该是"你传错了"的错误会全变成 500。
4. **八字 verdict 是「身强/身弱」，属状态而非吉凶**：前端必须走中性色。
   `DuanCard.verdictTone` 用白名单（只认「偏吉/偏凶」）——
   把「身弱」画成红色 = 把中性事实渲染成凶兆，属 RULE-008 违规。

### 前端日期：单一实现点

新增 `apps/mobile/src/lib/date.ts`，全仓唯一日期换算点。
🔴 **任何页面都不许写 `toISOString().slice(0,10)`** ——
`new Date(2026,8,17,0,30).toISOString()` 在 UTC+8 得到 `2026-09-16`，
**凌晨 0:00–8:00 会退一天**，且只在一天的前 1/3 出现，极难发现。
回归守卫 `tests/mobile/test_local_date.py`（26 例，真跑 TS 而非扫源码）。

### 假绿自检（本轮最值得记住的一段）

`test_local_date.py` 的陷阱能否复现**取决于运行时区**：在 UTC 或西半球
`toISOString` 并不退一天，所有断言会轻松通过，测试已成空话。
三重防线：探针固定 `TZ=Asia/Shanghai`；断言偏移确为 +480；
断言"错误写法确实算错"至少一例。任一不成立则**报错而非跳过** ——
一个静默跳过的守卫等于没有守卫。

### 🔴 更正上一轮的结论：md5 自比对可能证明不了任何东西

上一轮（第七件事）把"还原后 md5 与变异前一致"当作还原成功的凭据。
本轮做 DuanCard 变异验证时踩到反例：在**同一条 `&&` 链**里做 `md5sum` 比对，
两边的求值对象可能**都是已改坏的**（还原 cp 与 md5 的先后不如你所想），
比对"相等"毫无意义 —— 当时 grep 才发现
`if (verdict === '身弱') return 'bad';` 还留在第 66 行。
**正确做法：md5 比对只作辅助，最终必须用内容断言（grep 关键片段）确认还原。**

### 契约测试扩到新接口

`tests/mobile/test_types_contract.py` 从 26 → 48 例（`_CHECKED` 38 项）。
两条跨层守卫值得记住：
- `test_duan_liuyao_carries_traceable_basis`：断卦必须带 `detail.basis`
  （起卦日 / 日辰 / 月令 / 用神）。缺了它，补录隔夜的卦会拿到一套
  依据全错的结论，而结论看起来完全正常。
- `test_frontend_verdict_whitelist_matches_kernel`：**直接解析 `DuanCard.tsx`
  源码**抽出前端判为吉凶的词，与内核常量对撞（不抄第二份常量，否则是
  两个我在互相对答案）。两种漂移都不报错：前端多认一个词会把「身弱」
  画成凶兆；内核改词而前端没跟会把「偏凶」画成中性（只会颜色不对，
  最容易被当成"设计如此"放过）。

### 验证（[已确认]，附实际命令与输出）

```
$ "$PY" -m pytest --collect-only          → 940 tests collected in 1.87s
$ "$PY" -m pytest（分模块核 exit code）    → 940 passed，零回归
$ cd apps/mobile && npm run typecheck      → 零错误
$ cd apps/mobile && npx expo export --platform android
                                           → 产出 3.32MB .hbc，通过
$ "$PY" scripts/smoke_api.py + 管理台 E2E  → 62 条断言全过（uvicorn 8363）
$ 变异：toISODate 改回 toISOString         → 14 项失败（已还原）
$ 变异：DuanCard 白名单加「身弱」           → 跨层守卫失败（已还原）
```

基线 834 → **940**（+12 契约 +26 日期 + 断卦/管理台 E2E 等）。
`--collect-only` 的 940 与文档基线数字一致，已交叉核对。

> ⚠️ pytest 汇总行会被 WorkBuddy 的 `[safe-delete]` 吞掉（清理临时目录触发），
> 表现为**全绿却 exit=1**。核验方式：**分模块跑、按 exit code 判断**，
> 且**不要重复传 `-q`**（pyproject 的 addopts 已含 `-q`，再传变 `-qq` 会吞汇总行）。

### 剩余问题（[未验证] / [待办]）

- 🔴 **真机 UI 走查未做**（HANDOFF §7.1 已显式列出）：`/almanac` 路由跳转、
  布局溢出、窄屏 chip 换行、大运网格在小屏的排布、"3 秒内确认"的手感、
  键盘遮挡输入 —— 这些**只能在真机/模拟器上测**，桌面端 tsc + export 证不了。
  这是当前"能实战"唯一未被证伪的一环。
- **三式（奇门/六壬/太乙）仍未开始** —— 全项目唯一剩余空白。
- 项目仍**无 remote**。
- 🔴 [tooling] 命令回显与仓库实际状态矛盾时，必须以 git log / git show 的实际输出为准，不可据单次回显判定成败 —— 执行 git commit 等写操作类命令之后；因为命令可能被重复执行或输出被截断，回显与事实可以不一致（实测：commit 5d3a429 确实已创建且内容正确，命令回显却是 "nothing to commit, working tree clean"），若据此判定为失败并重做，会产生重复提交或误报状态。  <!-- 17:43 -->

---

## 19:50–20:40 第六轮：APK 与 WEBUI 端到端可用性（阿勇：「前端APK，后端WEBUI呢」）

### 结论：两者都已存在，但都没到「能直接用」

| 事实 | 证据 |
|---|---|
| `dist/` 两个 APK **完全不含三式** | 解包 grep「大六壬」「奇门」「liuren/cast」「qimen/cast」全部命中 **0 次** |
| 两者内联地址不同 | `-oc.apk` → `oc.ayong.qzz.io:8360`；`-release.apk` → `192.168.57.10:8360` |
| `oc.ayong.qzz.io` **已无法解析** | `nslookup` 无记录、`ping -6` 报「找不到主机」 |
| 容器是 **21 小时前**的旧镜像 | 容器内 `grep -c admin app.py` = **0**；`/admin` 返回 **404**（代码里"文件缺失"时才 503） |
| 本机有 **3 个**可用局域网 IPv4 | 192.168.57.10（以太网3）/ 192.168.59.56（以太网）/ 192.168.68.80（以太网2） |

### 做了什么

1. **重建容器镜像**：`/admin` 404 → **200**（30KB，8 张面板全在），
   配 `.env` 的 `XUANPAN_ADMIN_TOKEN` 后 `/api/v1/admin/overview`、`/sessions` 均 200。
   🔴 **改了后端代码后只 `docker compose up -d` 不 `--build`，会继续跑旧镜像** —— 本轮故障根因。
   另：403 = 令牌**没配**（功能整体关闭）；401 = 配了但**值不对**。这个区分能省很多排查时间。
2. **APK 多候选地址机制**（`8c59adb` 运行期 + `73b0b45` 构建期）
   - 新增 `apps/mobile/src/lib/apiCandidates.ts` —— 纯函数、**零 RN 依赖**，
     所以能被真跑测试（写在 client.ts 里则永远只能人工点页面验证）
   - 启动**并发**探活候选表，取第一个回应者；全失败返回 null **不抛**
     （抛 = 红屏 → 用户连「我的 → 网络线路」手改入口都进不去）
   - 构建脚本自动枚举局域网 IPv4；`oc.ayong.qzz.io` 从默认首选降为候选末位
3. **13 例新测试 + 变异 3/3 捕获**；`tests/mobile` 166 → **179 passed**；`tsc` 零错

### 🔴 本轮最值钱的坑：变异脚本自己的「假捕获」

上一轮立的模式是「改坏实现 → 跑测试 → 非零退出码即算捕获」。**这个判据不可靠**：

- `-k` 表达式用 `|` 分隔（bash 思维）→ pytest 报 `Wrong expression` 并 **exit=4**，
  而 exit=4 与「测试失败」的非零码长得一模一样 → 被计入捕获，得到**假的 3/3**
- `-k` 没匹配到任何用例时 pytest 同样返回非零 → 也会被误记

**修正后的判据**：只有「真的收集并执行了用例」且「有用例失败」才算捕获；
`-k` 的布尔语法改回 `and/or/not`。

> **教训：验证工具的「通过/失败」判据本身也要被验证。**
> 一个只看退出码的变异脚本，会把「参数错误」「收集为空」「超时」全部读成"成功捕获"。

配套：变异锚点必须**唯一** —— `if (url && !out.includes(url)) out.push(url);`
在两个函数里各有一份，只给这一行会因出现 2 次而被静默跳过（本轮就跳过了一个，
脚本因此报 `捕获 2/3`；改为带上一行上下文后才唯一定位）。

### 附带发现的待办（已写入 HANDOFF §2.4）

- `/qimen/pan` 与 `/liuren/cast` 路径**命名不一致**（一个 `pan` 一个 `cast`）——
  不影响功能，但 Agent/新人容易猜错（本轮就猜错了一次，误判成 404 bug）
- `fenjin120` 规则表缺失（管理台 `available=False`）—— 已知数据缺口，
  当前退化为「只输出几何格位 + 警告」，非本次引入
