# 玄盘AI · 项目长期记忆

## 项目基本盘

- **定位**：AI 应用项目（与 A 股 / 股票业务无关，勿与「龙息 · 全息监控引擎」混淆）
- **技术栈**：**尚未定型**（截至 2026-09-16）。禁止预设语言与框架
- **仓库**：`D:\WorkBuddy\玄盘AI`，默认分支 `main`，尚无 remote

## 开发规范（根目录 AGENTS.md 为最高规范）

🔴 **新会话必须先完整阅读 `AGENTS.md`** —— 因为 AI 无跨会话记忆，仓库状态与 git 历史是唯一记忆载体

两条铁律：

1. **每次改动必须创建对应的单独 commit**，一个 commit 一个逻辑单元，禁止攒批提交；禁用 `git add -A`，必须精确暂存
2. **每次改动必须同步测试，交付前测试与验收全部通过**，且交付时必须附上**实际执行的命令 + 真实输出**；未实际运行不得声称「测试通过」

配套约定：

- 提交信息格式 `<type>(<scope>): <中文简述>`，type 用英文（feat/fix/docs/refactor/test/chore/perf）
- 结论必须标注 `[已确认]` / `[推测]` / `[待验证]`
- 交付固定四段：【完成内容】【修改文件】【验证结果】【剩余问题及下一步】
- 引入任何新依赖前，必须先说明：为什么用、有无更轻替代、**成本（是否收费 / 是否需 API key）**、对现有代码影响

## 环境

- git 全局身份：`ayongsheng777-rgb` / `277914440+ayongsheng777-rgb@users.noreply.github.com`
- 全局 `user.name` 曾缺失（2026-09-16 补上），此后新建仓库不会再报 `Please tell me who you are`

## 待回填

- AGENTS.md §5 技术栈章节 —— 技术栈一旦确定必须立即回填，回填后 §2.2 的「测试」即指该章节登记的测试命令
