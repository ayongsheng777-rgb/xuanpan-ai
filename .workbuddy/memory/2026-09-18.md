# 2026-09-18 工作日志

## V2 改造断点交接（08:10）

前一账号积分耗尽，V2 改造进行中交接给下一账号。

**V2 已完成（6 commit，全部已推送 remote）**：
- `6b3a27f` V2 规范入库 + 评估与实施路线
- `6b9666f` tokens 增加 instrument 深色仪器色域
- `ff57318` CompassDial 双轨调色板（DIAL_LIGHT/DIAL_DARK）
- `da1472b` sensorQuality.ts 质量评估库 + 22 锚点测试（变异验证过）
- `09be135` 罗盘首页深色仪器风（演示图第 1 屏）
- `e90f135` /adjust 手动调节罗盘页（演示图第 2 屏）

**断点（剩 3 件，详见 HANDOFF §2.5）**：
1. `/sensors` 传感器测量页（演示图第 3 屏）—— useSensors.ts + sensorQuality.ts 已就绪，只差 `apps/mobile/app/sensors.tsx` 页面本身
2. 验证：typecheck + expo export + tests/mobile
3. 文档/记忆更新 + 分逻辑单元提交

**边界（别越界）**：底部导航不动；不做全局深色化（罗盘域深色/其余浅色双轨）；V2 Phase 3+ 暂缓。

**其他**：remote 已建（GitHub 私有库 xuanpan-ai），main 已推至 `e90f135`；基线 tag `v0.1.0-pre-v2`。

---

## V2 罗盘域改版收尾（09:03–09:25）

接手 V2 断点（HANDOFF §2.5），把剩余三件事做完。

**新增 3 commit**：

- `f0d21d0` `lib/sparkline.ts` 磁场曲线几何纯函数 + 探针 + 17 锚点测试
  （变异验证：去掉跨度保护 → 7 failed，还原后 17 passed）
- `95bfa7c` `/sensors` 传感器测量页（演示图第 3 屏）+ `help.ts` sensors 讲解主题
- `350c78e` 罗盘首页讲解文案同步 —— 原文案还在教用户点 V2 已删掉的「拍摄罗盘」按钮

**/sensors 守住的三条语义**（已写进 `sensors.tsx` 文件头注释）：

1. 无数据是一等状态 → 显示「—」+ 提示，**盘面不渲染**
   （画一个静止在 0° 的盘会被读成"当前方位就是 0°"）
2. 读数相对**磁北**、未做磁偏角改正 → 盘面山格只作方向参考，**不构成坐向结论**
3. 「磁场质量」不叫"精度" —— 那个 0~100 分只衡量强度是否在区间 + 波动大小

**验证**：`tsc --noEmit` 零错；`expo export` 出包 3.43MB；`.hbc`（UTF-16LE）字符串表命中新页面特征串；
分模块测试合计 **1411 passed, 2 skipped**（1372 +22 传感器库 +17 曲线库，账对得上）。

**本轮新踩的坑（已固化进 MEMORY.md）**：

- `expo export --output-dir /tmp/x` 在 Windows 落到**当前盘符**的 `\tmp\x`（实测 `D:\tmp\`）
  → 随后 `cd /tmp/x` 报 No such file，会**误判成打包失败**
- `.hbc` 里中文串是 **UTF-16LE**，用 utf-8 grep 得 **0 命中（假阴性）** → 必须带旧页面串做对照组
- `[safe-delete]` 吞 pytest 汇总行（222 文件 > 阈值 50）→ 用 `--basetemp=D:/tmp/pytest-base`
  绕开，拿到真实 `827 passed, 2 skipped`

**遗留**：`/adjust` 页无讲解入口（记入 HANDOFF §2.5 遗留）；真机 UI 走查仍未做，V2 又增 3 页。

**记忆维护**：MEMORY.md 超限被截断 → 重写瘦身 **33%**（10284 → 6925 字符），
🔴 规则数 25 → **31**（并入本轮新坑）。
- 🔴 [打包] `expo export --output-dir /tmp/x` 在 Windows 会写到**当前盘符**的 `\tmp\x` —— 核验打包产物时；因为 Node 把它当 Windows 路径解析，cwd 在 D 盘就是 `D:\tmp\`，既不是 Git Bash 的 `/tmp` 也不是 `C:\tmp`；随后 `cd /tmp/x` 报 No such file，会把成功误判成「打包失败」。按 Expo 打印的路径配合当前盘符定位。  <!-- 09:20 -->
- 🔴 [打包] Hermes 字节码 `.hbc` 里的中文串以 **UTF-16LE** 存储 —— 想证实「某页面/某文案真的进了包」时；用 utf-8 grep 会得到 0 命中（假阴性），从而误判成「页面没打进包」。改用 `bytes.count(s.encode('utf-16-le'))`，并**带一个已知旧串做对照组**自证探测方法有效。  <!-- 09:20 -->
- 🔴 [测试] WorkBuddy 的 `[safe-delete]` 会吞掉 pytest 汇总行（teardown 清理临时目录、文件数超阈值 50 时触发）—— 跑测试看到 exit=1 时；现象是进度条走满 100%、一个 `F` 都没有，紧跟一行 `[SAFE_DELETE_BULK_CONFIRM_REQUIRED]` JSON，**实为全绿**。绕法：传入系统 Temp 之外的 basetemp（`--basetemp=D:/tmp/pytest-base`）即可拿到真实汇总行；判据看有没有 `F`/`E` 与该 JSON，**不能只看 exit code**。  <!-- 09:20 -->
