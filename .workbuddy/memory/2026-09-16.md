# 2026-09-16 · 玄盘AI 项目初始化

## 完成事项

1. **git 仓库初始化**
   - `D:\WorkBuddy\玄盘AI` 初始化为 git 仓库，默认分支 `main`
   - 初始化时目录完全空白（无代码、无配置文件）

2. **补齐 git 全局身份**
   - 发现全局仅有 `user.email`、**缺 `user.name`**，会导致首次 commit 直接失败
   - 经确认后设置：`user.name = ayongsheng777-rgb`（与 GitHub 账号一致）
   - `user.email = 277914440+ayongsheng777-rgb@users.noreply.github.com`

3. **建立 AGENTS.md 项目 AI 开发规范**（174 行）
   - 保留用户原有的两条铁律，并强化为可判定条款
   - 新增：会话启动检查、六步工作流程、状态标注规则、红线、交付格式、目录约定、自我验证方式
   - 技术栈章节登记为「未定型 + 待回填」，并规定引入依赖前必须说明成本

4. **首次 commit**：`0f85666 docs(agents): 建立项目 AI 开发规范`

## 关键决策

- **技术栈尚未定型** → AGENTS.md 不写死任何构建/测试命令，只规范行为与流程
- **业务方向**：AI 应用（与 A 股/股票无关，区别于「龙息」项目）
- 提交信息约定：Conventional Commits，`type` 用英文、简述用中文
- 禁用 `git add -A`，一律精确暂存

## 待办（20:43 更新）

- [x] ~~技术栈确定后回填 AGENTS.md §5~~ → 本轮已完成
- [ ] 补齐 `.gitignore`（代码落地前必须完成）
- [ ] 尚无 remote 仓库
- [ ] 三份材料 5 处不一致待阿勇最终裁定（导航栏/报告标签/色板/隐私文案/演示图数据）

---

# 2026-09-16 · 第二轮：产品方案评审与生产路线报告

## 输入材料

- `docs/xuanpan-ai-APP-project.md` —— 产品与架构方案（50 节）
- `docs/玄盘 AI — UI、模型配置与网络线路设计规范.md` —— UI/模型/网络规范（30 节）
- `docs/ChatGPT Image 2026年9月16日 20_11_06.png` —— 10 屏 UI 演示图

## 项目定位（已确认）

- **玄盘 AI**：罗盘视觉识别 × 确定性术数计算 × AI 解释的移动端 APP
- 核心不是「AI 算命」，而是**把实体罗盘转成结构化数字数据**
- 工程原则：**AI 负责看/理解/解释；代码负责算/校验/映射**

## 关键实证：演示图八字数据错误

- 演示图 4 出生信息：1981-09-14 08:00，男，湖北
- **实算**（lunar-python）：辛酉 丁酉 **乙未** **庚辰** ｜ 日主 **乙**
- **演示图标注**：辛酉 丁酉 **辛丑** **甲辰** ｜ 日主 **辛金**
- 三条证据链：
  1. lunar-python 实算
  2. JDN 公式 + 偏移 k=49（4 个日期恒定，2000-01-01 戊午日为公认锚点）
  3. 纯规则矛盾：辛日子时戊子 → 辰时应为**壬辰**，图却标甲辰，自身不成立
- **结论：演示图术数数据不可采信**，已写入 AGENTS.md §2.3 作为 RULE-001 的实证佐证

## 其他审计发现（5 处不一致）

1. 底部导航：材料A 5栏(历史) / 材料B 5栏(记录) / 演示图 3栏 —— 结构性冲突
2. AI 报告标签：材料A 四段 / 材料B 三标签 / 演示图 两标签
3. 配色：材料A 4 色值 / 演示图含藏青墨绿
4. **隐私文案「数据不外流」与「照片上传识别」架构自相矛盾** —— 合规与信任风险
5. 演示图八字数据错误

## 环境踩坑（可复用）

- 🔴 managed Python `venv` 创建后 `Scripts/` 只有 `python.exe`，**缺 pip/activate**（`ensurepip` 静默失败）→ 用 `python -m ensurepip --upgrade` 修复
- 🔴 `pip install --upgrade pip` 在 Windows venv 下报 `WinError 2` → 跳过自升级可直接装包
- 🔴 `sxtwl` 在 **Python 3.13 无预编译 wheel**，构建失败 → 改用 `lunar-python` + 自写 JDN 公式做**双源交叉验证**

## 产出

- `docs/玄盘 AI — 生产路线报告.md` —— 材料审计、冲突裁定、技术选型、Phase 0~5 路线、风险登记、实证附录
- `AGENTS.md` 更新三处：§1 定位与材料索引、§2.3 RULE-001~010、§5 技术栈回填

## commit

```
85c2c92 docs(agents): 回填技术栈并并入术数领域铁律
0fec903 docs: 新增项目生产路线报告
be8de08 docs: 收录玄盘 AI 产品方案材料
```

---

# 2026-09-16 · 第三轮：基线收敛（4 项裁定落地）

## 阿勇的 4 项裁定

1. 底部导航 → **方案 A：5 栏（罗盘 ｜ 命盘 ｜ 占测 ｜ 历史 ｜ 我的）**
2. AI 报告标签 → **方案 B：三标签（盘面事实 ｜ 传统分析 ｜ AI 解读）**
3. 配色 → **以演示图为主**
4. 相机入口 → **拍照 + 图片上传 双入口**

原话：「全部一步到位做完」

## 关键做法：配色不靠肉眼，从像素实测

- 演示图 1214×1295。色板位于 `y = 105..134`（最宽处 `y ≈ 120`），圆心 x = `731 / 768 / 804 / 840 / 877`
- 对每个圆心取 **9×9 邻域逐通道中位数**（规避抗锯齿边缘）
- 实测结果：`#013A6C` `#DAB37D` `#E5D7C7` `#3C7066` `#B93E35`
- **负向排除**：`y=120` 行还扫到 `x=920..952` 两个灰蓝段（`#8497A0`/`#7F929A`）→ 判定为右侧山水画像素，**不是色板**，剔除
- **有效性验证**：统计界面区（`y=300..1080`）有彩像素，5 色全部命中（3997 / 1577 / 2760 / 7347 / 567 px）→ 色板确为界面实际用色

> 方法论：**色值类数据一律脚本实测 + 双向验证（正向命中 + 负向排除）**，禁止肉眼取色。

## .gitignore 做了负向验证

创建 `.env` / `test.log` / `node_modules/x.js` → `git status` 中三者均未出现 → 规则确实生效；随后清理。

## 产出

- `docs/玄盘 AI — 产品基线规范.md` —— **唯一真源**：固化 4 项裁定 + 5 个 design token + 相机双入口统一管线
- `AGENTS.md` §1.1 登记该文件为唯一真源，冲突时以其为准
- 生产路线报告 §2.0 裁定汇总表、§8/§9 进度同步
- `.gitignore`

## commit

```
71f1590 chore: 新增 .gitignore
78bb5b4 docs: 同步生产路线报告的裁定状态与进度
720916d docs(基线): 新增产品基线规范并登记为唯一真源
```

## 遗留

- **隐私文案「数据不外流」与上传架构矛盾 —— 仍未修正**（路线报告 §2.4）
- Phase 0 编码待启动
- 🔴 [测试] 把 docstring 里的示例纳入自动化测试（doctest），别让文档示例只当注释放着 —— 任何用 docstring 承载『为什么这样算』的项目；因为示例与实现脱节后没有任何机制会发现它，本次在两个成熟模块里一次抓出两处潜伏假契约（一处期望值写错、一处实现了 argmax 取平台左端而文档要求落在两类之间），README 与注释被误导的代价远高于一条测试  <!-- 21:36 -->
- 🔴 [测试] 为几何/数值变换写测试时，必须包含『退化情形 + 非退化情形叠加』的组合用例，只测单一变换会放过符号/系数错误 —— 斜拍校正这类仿射变换；因为纯压缩(或纯旋转)场景下错误的旋转符号/系数会恰好互相抵消、测试全绿，一旦叠加第二种变换误差立刻飙到 40°~60°；本次正是靠『压扁+旋转』组合才暴露  <!-- 21:36 -->
- [视觉] 用二阶矩(协方差+eigh)估主轴方向前，先判长宽比是否退化（如 a/b < 1.02），退化时不要使用该方向 —— 从图像/点云拟合圆或椭圆时；因为近正圆的协方差近似各向同性，特征向量完全由浮点噪声决定，方向在数学上无定义；把它当旋转角施加会把整幅内容旋转一个任意角度，症状是『越规整的输入结果越离谱』  <!-- 21:36 -->
- 🔴 [架构] 把类实例的方法当值传出去（如 useSubmit(client.method)）前，先确认它已绑定 this；最稳的做法是让公开方法一律写成箭头函数属性 —— 因为普通类方法依赖调用时的接收者，传引用后 this 变 undefined，内部 this.xxx 抛 TypeError，而类型检查完全合法（不报错），只在用户真按下按钮时才炸；本次一次性出现在 9 个调用点上，说明问题在 API 形状而不在调用方粗心  <!-- 22:46 -->
- 🔴 [测试] TS 类型是手写的（未从后端生成）时，必须有一个用『真实响应』校验类型字段的测试 —— 因为字段名写错不会报编译错，只会让取值恒为 undefined（界面表现为某一格空了）；本次 raw_text 写成 text、quality 的 size:[w,h] 写成 width/height 两处都是这种方式抓到的；校验方向只做『TS 声明 ⊆ 真实响应』单向，反向不加断言以免后端每加字段都要改前端  <!-- 22:46 -->
- [安装] npm 安装依赖十几分钟没输出且 node_modules 一直不出现时，先 curl 一下 registry，不要以为是『依赖本来就大』—— 因为 registry 不可达时 npm 会静默卡在依赖解析阶段、不报错；本次实测 registry.npmjs.org 返回 000、registry.npmmirror.com 200/0.14s；修法是在项目里提交 .npmrc 固定镜像，而不是每次手打 --registry  <!-- 22:46 -->
- 🔴 [docker] 在 Windows 宿主机上写 Dockerfile 装 Python 包时，先显式指定国内 PyPI 镜像（可用 ARG 做成可覆盖），别指望默认源 —— 因为本机可能连宿主都解析不了 pypi.org（宿主靠 HTTP_PROXY 让代理远端解析域名才装得上），而容器内没有代理环境变量，直连必然 Temporary failure in name resolution；症状是 docker build 只回一句 CalledProcessError 加 exit status 1，把「装不上包」伪装成「pip 参数写错了」，实际要看容器内直接跑 pip 才看得到真因  <!-- 23:13 -->
- 🔴 [网络] 用 httpx/requests 之类的客户端连本机服务做联调时，显式设 trust_env=False 禁用代理环境变量，并先加一次探活再跑用例 —— 因为这类库默认 trust_env=True 会读 HTTP_PROXY/HTTPS_PROXY（本机确实设了），把 127.0.0.1 的请求也交给代理；服务没起时代理回一句 upstream connect failed 加非 JSON 正文，脚本再调 r.json() 抛 JSONDecodeError，报错指向 JSON 解析而与真实原因完全无关  <!-- 23:13 -->
- 🔴 [测试] 写「贴边/越界/遮挡」这类布尔判据时，用几何或比例条件，绝不用「区域里存在任意一个满足条件的元素」 —— 因为后者在噪声输入下必然恒定误报（本例：掩膜边缘 2px 条带里有一个噪点像素就判「罗盘未完整进入画面」），而真实照片必定带噪点；更糟的是误报附带的补救指引（调整拍摄距离）让用户永远修不好，同时把真实失效模式掩盖掉；修完实测命中率 63.7% → 80.0%  <!-- 23:13 -->

---

# 2026-09-16 · 第四轮：Task 6 收尾 + Task 7 最终交付

## Task 6 收尾（4 个 commit）

补齐移动端的跨语言守护与文档，工作区清空：

```
cb868d8 test(mobile): 环形选择器几何 ↔ 后端山表 跨语言一致性校验
a1f9295 test(mobile): TS 类型声明 ↔ 真实响应 契约校验
a145138 docs: 回填移动端构建与校验命令
c05d1fe chore(memory): 记录移动端闭环与两道一致性守卫
```

## Task 7 交付（8 个 commit）

```
9d65fcd fix(vision): 贴边判据改为几何判据，避免噪点误报「罗盘未完整入镜」
1be3e8a feat(tools): Gate 1 评测工具（合成退化扫描 + 真实照片评测）
06b6dc2 docs(gate1): 合成退化扫描实测记录（96 例）
85ab434 docs(agents): 回填 Gate 1 评测命令与失败分类判读规则
7c4da40 fix(tools): 冒烟脚本改用直连并给失联以可照做的提示
883d5f8 fix(chore): .gitignore 补齐 SQLite 运行时库，修正误导性注释
4183d38 feat(deploy): 容器编排、镜像与穷尽式环境变量样例
e9b2b9e docs: 同步路线报告的 Gate 1 实证、新风险与下一步
```

## 本轮最有价值的产出：Gate 1 从「无法评测」变成「有可复现证据」

建了 `scripts/gate1_eval.py`，12 档退化 × 8 真值角 = 96 例。首个跑出来的结果就
直接暴露了一个**会让真机全线失败**的缺陷（R9）：

- 贴边判据原是「掩膜边缘 2px 条带里有任意一个前景像素即判贴边」
- 真实照片必带噪点 → 边缘杂散像素 → **恒定报「罗盘未完整进入画面」**
- 而补救指引是「调整距离让整个圆盘入镜」，盘体本来就在画面内 → **永远修不好**
- 诊断关键：同一张图半径估计 270.5px vs 真值 268.8px（差 0.6%），说明检测是对的，
  是判据错了；「小图」单独 5/5、「噪点」单独 5/5、叠加 0/5，进一步坐实
- 修复后：噪点 3/8→8/8，组合·随手拍 0/8→8/8，合计 63.7%→80.0%，角度精度无损

## 新发现的高危风险 R8：假阳性

96 例里**识别到盘体 80 例、命中仅 65 例** → 15 例给出了山名候选但山名是错的
（实测真值 45°=艮/坤 被返回为乙/辛）。诱因是背景杂物（深色物件/桌面边缘）污染
前景掩膜 → 质心偏移 → 极坐标展开错位。

**这类失败与「识别不出」性质完全不同**：用户看到的不是「识别不出，请重拍」，
而是一个看似正常的错误坐向，不会去重拍。已在报告 §7 登记为 R8（🔴 高），
并把「假阳性」做成评测脚本的一等指标（不与识别率平均）。

## 交付物实测结果（都实际跑过，非声称）

| 项目 | 结果 |
|---|---|
| 全量测试 | 626 passed（内核 403 + tests/ 223） |
| 端到端冒烟（宿主 uvicorn） | 34 通过 / 0 失败 |
| 端到端冒烟（容器） | 34 通过 / 0 失败 |
| 移动端 tsc | 0 错 |
| 容器 | healthy，8360→8352，非 root（uid 10001） |
| 命名卷持久化 | 建会话 → 重启容器 → 会话仍在 |
| 构建上下文 | 385MB → **1.17kB**（实测） |
| 卷内容 | 仅 xuanpan.db（81KB），无图片 → keep_photos=false 实证 |

## 顺带修正的两处不一致

1. `.gitignore` 漏了 `data/*.db`（本地开发库会被误提交），且注释称根 `data/` 装
   「领域数据」与实际不符 —— 实测三个领域表的默认路径全在
   `packages/fortune-core/data/`。已双向验证忽略规则。
2. 冒烟脚本两处：httpx 默认 `trust_env=True` 让 127.0.0.1 也走代理（报错指向
   JSON 解析，与真因无关）；无可达性探活。已改为 `trust_env=False` + 探活返回 2。

## 隐私表述核查（结项）

全仓 grep「不外流/不外传/不上传」→ 仅剩路线报告里「旧文案已修正」的历史记录，
无残留假承诺。移动端 `mine.tsx` 与 `scan.tsx` 的措辞均与「数据可外流」裁定一致，
`scan.tsx` 明确告知「照片会上传到 … 完成识别；默认不保留原图」。

## Gate 1 仍未通过 —— 卡在需要真实照片

合成图在原理上答不了 Gate 1。取证要求 ≥5 类拍摄条件 × 各 ≥3 张 = ≥15 张，
放 `real_photos/` + `labels.csv` 后跑 `photos` 子命令。缺样本时脚本返回 2 并打印指引，
不会给虚假的达标结论。

## 下一步（不依赖照片可先做的，按性价比）

1. 修 R8：掩膜抗污染（排除与盘体不连通的边缘连通域 / 鲁棒质心）
2. 先建假阳性回归基线（当前 15/96），否则修 R8 时无法判断是否真变好
3. 真机/模拟器 UI 走查 —— 「3 秒内确认」这个交互指标只能在真机测
4. 反光档专项（0/8，原因「未能定位鱼丝线」）

