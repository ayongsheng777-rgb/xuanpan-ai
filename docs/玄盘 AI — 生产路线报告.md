# 玄盘 AI — 项目生产路线报告

> 版本：V1.0 ｜ 日期：2026-09-16 ｜ 编制：腾仔
> 状态标注遵循 `AGENTS.md` §4：`[已确认]` / `[推测]` / `[待验证]`

## 0. 报告依据与方法

**输入材料**

| # | 材料 | 性质 |
|---|---|---|
| A | `docs/xuanpan-ai-APP-project.md` | 产品与架构方案 V1.0（50 节） |
| B | `docs/玄盘 AI — UI、模型配置与网络线路设计规范.md` | UI / 模型 / 网络规范 V1.0（30 节） |
| C | `docs/ChatGPT Image 2026年9月16日 20_11_06.png` | APP 界面演示图（10 屏） |

**方法**：三份材料交叉比对 + **代码实算验证**（附录 A）。不采信任何未经计算的术数数据。

---

## 1. 结论摘要

1. **产品方向成立，工程原则正确。** 核心是「罗盘数字化」，而非「AI 算命」。材料中反复强调的 `AI 负责看/理解/解释，代码负责算/校验/映射` 是本项目最有价值的资产 `[已确认]`。
2. **三份材料存在 5 处内部不一致**，其中 1 处是**确定性数据错误**（演示图八字排盘，见 §2.5）`[已确认]`。
3. **最大风险不是 AI 能力，而是罗盘视觉识别的可行性至今未经验证。** 它应当作为第一个 Gate，而非最后一道工序 `[已确认]`。
4. **建议维持「先内核、后 UI」的顺序**，见 §5。

---

## 2. 材料一致性审计

> 三份材料由不同阶段产出，存在漂移。以下逐项裁定，**建议以裁定列为准落地**。

### 2.0 裁定结果（2026-09-16 已裁决）

| # | 冲突项 | **最终裁定** |
|---|---|---|
| 1 | 底部导航 | **方案 A —— 5 栏：罗盘 ｜ 命盘 ｜ 占测 ｜ 历史 ｜ 我的** |
| 2 | AI 报告结构 | **方案 B —— 三标签：盘面事实 ｜ 传统分析 ｜ AI 解读** |
| 3 | 配色 | **以演示图为准**（已像素实测提取 5 个 token） |
| 4 | 相机入口 | **双入口：拍照 + 图片上传（相册）** |

> ✅ 以上 4 项已全部裁决并写入 **`docs/玄盘 AI — 产品基线规范.md`**（唯一真源）。本节以下各小节保留为**决策依据**，结论以 2.0 与基线规范为准。

### 2.1 底部导航（三处互不一致）

| 来源 | 五栏 / 三栏 |
|---|---|
| 材料 A §18 | 罗盘 ｜ 命盘 ｜ 占测 ｜ **历史** ｜ 我的 |
| 材料 B §二 | 罗盘 ｜ 命盘 ｜ 占测 ｜ **记录** ｜ 我的 |
| 材料 C 演示图 | **首页 ｜ 记录 ｜ 我的**（仅 3 栏） |

**裁定（已裁决）**：`[已确认]` 采用**方案 A —— 5 栏：罗盘 ｜ 命盘 ｜ 占测 ｜ 历史 ｜ 我的**。演示图的三栏结构作废；第 4 栏统一命名「**历史**」（非「记录」）。详见《产品基线规范》§1。

### 2.2 AI 报告页信息结构（三种方案）

| 来源 | 结构 |
|---|---|
| 材料 A §16 | 【盘面事实】【传统术数解读】【针对你的问题】【参考建议】 |
| 材料 B §23 | 三标签【盘面事实】【传统分析】【AI解读】 |
| 材料 C 演示图 5 | 两标签【简要报告】【详细解读】 |

**裁定（已裁决）**：`[已确认]` 采用**方案 B 的三标签：盘面事实 ｜ 传统分析 ｜ AI 解读**。理由：它与 `FACT / TRADITION / AI_INTERPRETATION` 分层一一对应，是唯一能把「计算」与「解释」在 UI 上**物理隔离**的方案。演示图两标签作废；材料 A 的四段结构（针对你的问题 / 参考建议）并入 AI 解读标签作为子区块。详见《产品基线规范》§3。

### 2.3 配色

- 材料 A §22：`#F5F0E8` `#2C2416` `#C41E1E` `#8B7355`
- 材料 C：藏青 / 米白 / 赭红 / 墨绿系

**裁定（已裁决）**：`[已确认]` 采用**演示图色板**，并已从像素**实测提取**为 5 个 token：

| Token | 色值 | 语义 |
|---|---|---|
| `--brand-primary` | `#013A6C` | 主色 · 深蓝 |
| `--brand-gold` | `#DAB37D` | 金 · 辅色 |
| `--brand-sand` | `#E5D7C7` | 米 · 底色 |
| `--brand-jade` | `#3C7066` | 墨绿 · 辅色 |
| `--brand-cinnabar` | `#B93E35` | 朱红 · 强调 |

已通过界面采样验证 5 色**均为实际在用色**（提取方法与验证见《产品基线规范》附录 A/B）。材料 A 的 4 色值转为备选存档，可用于派生中性色阶。

### 2.4 ✅ 隐私表述已修正（架构为准，不委屈事实）

- 演示图「设计理念」原写：**本地加密存储、数据不外流**
- 实际架构（材料 A §6 / 材料 B §15）：**照片上传 → 服务端 Vision 识别**

**裁定（已执行）**：`[已确认]` 用户裁定「**数据可外流**」—— 即承认照片出端这一事实，
按真实架构措辞，**不为了迁就旧文案而改动架构**。文案改为可被实现证实的写法：

> 「罗盘照片**仅用于识别**，分析完成后默认不保留原图；生辰信息仅用于排盘，
> 可随时一键删除全部记录。」

**为什么这样写**：每一句都能被代码验证 ——
- 「默认不保留原图」= `Settings.keep_photos=False`（默认值），原图只在内存过一遍
- 「可随时删除」= `DELETE /api/v1/sessions/{id}`，级联删除报告与对话（RULE-008 删除权）
- 「仅用于识别 / 排盘」= AI 的唯一输入是 `FortuneContext.to_ai_payload()`，
  看不到照片，也拿不到原始生辰文本（RULE-002）

**前者的「可验证」是关键词——用户能自己验证，才叫承诺。**

**实现落点**：`services/api/xuanpan_api/config.py`（`keep_photos` 默认 False）、
`routers/scan.py`（仅当显式开启才落盘原图）、`routers/sessions.py`（删除接口）。
`tests/api/test_api.py::TestScan::test_scan_does_not_persist_photo` 断言这一条。

### 2.5 ⚠️ 演示图八字排盘数据错误（代码实算）

演示图 4「八字排盘」展示：出生 `1981-09-14 08:00`，男，湖北 →

| 项 | 演示图 | 实算结果 | 判定 |
|---|---|---|---|
| 年柱 | 辛酉 | 辛酉 | ✅ |
| 月柱 | 丁酉 | 丁酉 | ✅ |
| **日柱** | **辛丑** | **乙未** | ❌ |
| **时柱** | **甲辰** | **庚辰** | ❌ |
| **日主** | **辛金** | **乙（木）** | ❌ |

**两条独立证据链**（详见附录 A）：

1. **纯规则自洽性**（不依赖任何数据库）：时柱由日干推出，口诀「丙辛从戊起」——辛日子时戊子，顺推至辰时应为 **壬辰**。因此演示图自身就是矛盾的：**它标称日主为辛，却给出甲辰时柱**。
2. **双源计算**：`lunar-python` 与「标准儒略日公式 + 偏移 k=49」两条独立路径给出**完全相同**的结果 `乙未日 / 乙木日主 / 庚辰时`，且 `k=49` 在 4 个测试日期上恒定，并被 `2000-01-01 = 戊午日` 这一公认锚点验证。

**裁定**：`[已确认]` 演示图数据为错误数据，**不得作为实现基准**。此案例恰好构成对 RULE-001（确定性计算必须由代码完成）的最佳注脚：**人工编造的术数数据，会以肉眼无法识别的方式出错**——连演示图都没能幸免。

---

## 3. 目标架构（确认版）

### 3.1 主链路

```text
Camera → Vision → Human Confirm → Fortune Core → Fortune Context → AI Router → Report → Multi-turn
```

### 3.2 分层职责

| 层 | 职责 | 关键约束 |
|---|---|---|
| 采集层 | 拍照、质量检测、透视校正、ROI 裁剪 | 客户端预处理器化，降低上传体积与 Vision 成本 |
| 识别层 | 罗盘 → 结构化 JSON | **不确定必须返回 null**（RULE-003） |
| **计算层** | 二十四山 / 坐向 / 分金 / 八字 / 五行 / 六爻 等 | **唯一真源，确定性，禁 LLM 介入** |
| 上下文层 | 组装 `FortuneContext` | AI 只见结构化数据，不见原始输入 |
| 解释层 | AI Router + Provider 抽象 | 只能解释，不能修改任何计算值 |
| 接入层 | REST API / MCP | APP、MCP、未来 Agent 共享同一内核 |

**架构核心价值**：`Fortune Core` 是被 APP / API / MCP / Agent 共享的**唯一计算内核**，不是为某个端写的代码。

---

## 4. 技术选型

| 层 | 方案（材料推荐） | 评估 | 备注 |
|---|---|---|---|
| Mobile | React Native + Expo（需强相机 → + VisionCamera） | `[推测]` 合理 | 若重相机能力，VisionCamera 几乎是必选项 |
| Backend | Python FastAPI | `[推测]` 合理，与 fortune-core 同语言 | 计算内核用 Python 可直连 `lunar-python` |
| 计算内核 | 自研 `packages/fortune-core` | `[已确认]` 必要 | 不内嵌 MCP Server，只吸收其算法思想 |
| 历法引擎 | `lunar-python` / `sxtwl` 二选一 | **`lunar-python` 已验证可用** | `sxtwl` 在 Python 3.13 无预编译 wheel，编译失败（附录 A） |
| 数据库 | PostgreSQL | `[推测]` 合理 | |
| 缓存 | Redis | `[推测]` 合理 | 主要用于 AI / Vision 结果缓存与限流 |
| 对象存储 | S3 / MinIO | `[已确认]` 必要 | 照片不进数据库 |
| 模型接入 | Provider 抽象（能力维度：Vision / Reasoning / Fast / Fallback） | `[已确认]` 关键设计 | **按能力而非品牌设计**，是全文最重要的架构决策之一 |

### 4.1 依赖成本前置说明（AGENTS.md §5 要求）

| 依赖 | 成本 | 说明 |
|---|---|---|
| `lunar-python` | **免费**（MIT） | 已装并验证 |
| `sxtwl` | 免费 | 但 Py3.13 需自行编译，建议按需降级 Python 版本 |
| Vision 模型 API | **按量计费** | 项目最大成本项，需预算 |
| LLM 解释 API | **按量计费** | 次大成本项 |
| 本地模型 | 一次性硬件成本 | 可作为成本兜底方案 |

> 引入任何计费依赖前，按 AGENTS.md §5 先做成本说明并取得确认。

---

## 5. 生产路线图

```text
Phase 0 ──► Gate 0 ──► Phase 1 ──► Gate 1 ⚠️关键 ──► Phase 2 ──► Phase 3 ──► Phase 4/5
计算内核      测试全绿    识别Demo     识别可用性         AI解读      移动端MVP      扩展能力
```

### Phase 0 — 计算内核（当前推荐起点）

**交付物**：`packages/fortune-core`

- 二十四山数据模型（唯一标准表，作为**领域配置**而非散落在 UI/Prompt）
- 坐山 / 向山模型 + 几何关系校验（坐山≠向山、应处相对方位）
- 角度 ↔ 二十四山映射
- 八字数据模型 + 历法引擎接入（公历/农历/节气/时区/真太阳时）
- 五行统计、日主、纳音
- `Fenjin120` 接口（V1 仅接口，V2 补规则表）
- 统一 `FortuneContext`
- 完整单元测试

**Gate 0 验收**：`pytest` 全绿；核心算法测试覆盖率达标；**零 LLM 参与计算**。

### Phase 1 — 罗盘识别 Demo

**这是全项目最关键的一步，也是最大不确定性所在。**

- 拍照 → 客户端预处理（清晰度/反光/透视/裁剪）
- Vision Provider 抽象 + 至少 1 个实现
- 输出结构化 JSON（含 `confidence` 与 `needs_user_confirmation`）
- 人工确认 / 手动修正（二十四山环形选择器）

**Gate 1 验收**（直接沿用材料 A §50 的判据）：

> **普通用户拿手机拍一张真实罗盘照片，系统能否稳定识别鱼丝线对应的二十四山，并让用户在 3 秒内确认。**

**⚠️ 此 Gate 不通过，则后续全部设计需重新评估。** 建议尽早用最小成本证伪。

### Phase 2 — AI 解读
`FortuneContext` → LLM → 三层结构化报告（盘面事实 / 传统分析 / AI 解读）。含 AI 幻觉防护与不确定性标注。

### Phase 3 — 移动端 MVP
首页、扫描、确认、命盘、报告、历史、删除数据。

### Phase 4 / 5 — 能力扩展与 MCP
六爻 / 灵签 / 姓名 / 起名 → `xuanpan-mcp` 发布。

---

## 6. 网络线路策略

材料 B §13–§19 将「网络线路」定位为 **AI 服务连接线路**，而非代理工具。这个定位是正确且必要的。策略要点：

1. **优先国内直连**：DeepSeek / Qwen / 智谱 / 豆包 作为**默认线路**，覆盖绝大多数场景，无跨境链路依赖。
2. **官方 API Gateway 模式**：模型调用统一走自建 Gateway（鉴权 / 限流 / Router / 成本统计），**API Key 永不进入客户端**（材料 B §27）。
3. **合规前提**：材料 B §16 已明确「线路、API 使用和服务部署需符合当地法律法规及模型服务商条款」。落地时必须遵守该前提 `[已确认]`。
4. **故障降级**：主模型失败 → 自动切换备用；**但模型切换绝不能改变 Fortune Core 的计算结果**（材料 B §21–§22）。
5. **对用户隐藏复杂度**：普通用户只见「已连接 / 延迟 / 自动选择」，技术参数收进「高级网络设置」。

---

## 7. 风险登记

| # | 风险 | 等级 | 影响 | 缓解措施 |
|---|---|---|---|---|
| R1 | **罗盘识别可行性未验证** | 🔴 高 | 产品根基不成立 | 提前做 Phase 1 最小 Demo，**先证伪** |
| R2 | Vision + LLM 成本失控 | 🔴 高 | 商业模式不成立 | 客户端预处理 + 缓存 + 分级调用（材料 A §40） |
| R3 | 隐私表述与架构矛盾 | 🟠 中高 | 信任 + 合规风险 | 立即修正文案（§2.4） |
| R4 | 三份材料不一致导致返工 | 🟠 中高 | 开发期混乱 | 本报告 §2 逐项裁定，落地前锁定 |
| R5 | `suanming-mcp` License 与署名 | 🟡 中 | 法律风险 | **仅借鉴架构思想，独立实现核心代码，保留来源说明**（材料 A §48） |
| R6 | AI 幻觉污染术数结论 | 🟠 中高 | 产品可信度崩塌 | RULE-001/002 + 三层报告结构隔离 |
| R7 | 演示图错误数据被当作基准 | 🟡 中 | 引入错误 | 已实测纠正（§2.5） |
| R8 | **识别假阳性**：返回了山名候选，但山名是错的 | 🔴 高 | 用户拿到一个**看似正常的错误坐向**（与「识别不出」的体验完全不同，不会去重拍） | ① 保持 `needs_user_confirmation=True`（RULE-004，当前唯一防线）② 把「假阳性」单列为评测一等指标，不与识别率平均（`scripts/gate1_eval.py`）③ 修掩膜抗污染（背景杂物是实测高发诱因，见 §8） |
| R9 | **识别闸门误报**：因噪点判定「罗盘未完整入镜」 | 🟠 中高 | 真机照片几乎必然触发 → Gate 1 恒不通过，且提示让用户调距离**永远修不好** | ✅ 已修复（贴边判据改为几何判据）；退化扫描 63.7% → 80.0%（§8） |

---

## 8. 仓库现状与缺口

**现状** `[已确认]`

```
玄盘AI/
├── .git/                      # 已初始化，main 分支，2 个 commit
├── AGENTS.md                  # 项目开发规范
├── .workbuddy/memory/         # 项目记忆
└── docs/                      # 三份方案材料（尚未提交）
```

**缺口与进展（Phase 0/1/2/3 已完成）**

- [x] ~~`.gitignore` 未建立~~ → **已建立**（覆盖密钥 / Python / Node-RN / 日志 / 运行时数据）
- [x] ~~AGENTS.md 技术栈章节待回填~~ → **已完成**（§5）
- [x] ~~材料不一致待裁定~~ → **4 项已裁决**（导航 / 报告标签 / 配色 / 相机入口），写入《产品基线规范》
- [x] ~~**隐私文案「数据不外流」尚未修正**（见 §2.4）~~ → **已修正**（用户裁定「数据可外流」，按真实架构措辞，见 §2.4）
- [x] ~~无任何代码~~ → **Phase 0~3 已落地**：
  - `packages/fortune-core` 确定性计算内核（403 测）
  - `services/vision` 罗盘识别管线（72 测）
  - `services/ai` AI 解释层（47 测）
  - `services/api` 后端服务（56 测）
  - `tests/mobile` 前后端一致性守卫（46 测）
  - 合计 **626 测全绿**
- [x] ~~`apps/mobile` 移动端进行中~~ → **已完成**：识向 → 确认坐向 → 三层报告 → 历史 全链路
  - 五栏信息架构（罗盘 / 命盘 / 占测 / 历史 / 我的），设计 Token 单源自演示图像素实测
  - 验证三层：`tsc --noEmit` 0 错；`expo export` 打包成功（3.24MB `.hbc`）；46 项一致性测通过
  - `[未验证]` **真机/模拟器 UI 走查未做** —— 只验证了打包与类型，未在设备上实走一遍
- [x] ~~`scripts/smoke_api.py` 对真实服务端到端通过~~ → **34 通过 / 0 失败**（2026-09-16 对 uvicorn 实进程复测）
- [x] ~~无容器编排~~ → **已交付并实测**：`docker-compose.yml` + `services/api/Dockerfile` +
  `.env.example`（穷尽 16 个环境变量）+ `.dockerignore`
  - 构建上下文 **385MB → 1.17kB**（实测，被 `.dockerignore` 挡掉的主要是 379MB 的 `node_modules`）
  - 容器内非 root 运行（uid 10001）；健康检查通过；**对容器跑端到端冒烟 34 通过 / 0 失败**
  - 命名卷持久化已实证：建会话 → 重启容器 → 会话仍在
  - 卷内只有 `xuanpan.db`（81KB）无任何图片 → 容器里 `keep_photos=false` 亦被证实
- [ ] 无 remote 仓库
- [ ] **Gate 1（真实照片可行性）仍未判定** —— 但已从「无法评测」推进到「有可复现评测」

### 8.1 Gate 1 当前实证（2026-09-16）

`scripts/gate1_eval.py synthetic`，12 档退化条件 × 8 真值角 = **96 例**：

| 指标 | 数值 |
|---|---|
| 坐向整对命中 | 65/96（67.7%） |
| 识别到盘体 | 80/96（83.3%） |
| **假阳性**（给了山名但都不对） | **15/96（15.6%）** |
| 角度误差 | 中位 0.01° / 最大 0.50° |

**两个结论**：

1. **几何算法已经够准** —— 角度中位误差 0.01°，连重斜拍档（squash=0.7 + tilt=25°）也只有 0.02°。
   瓶颈不在"算得对不对"，而在"输入能否被接受"。
2. **失败有两种性质，必须分开处理**：
   - 「明确拒绝」：失焦 0/8、反光 0/8 —— 返回可操作提示，符合 RULE-003，可接受
   - 「假阳性」：背景杂物档（贴边阴影 1/8、角落物件 0/8）—— **系统返回了错误山名却不自知**
     （实测真值 45°=艮/坤 被返回为乙/辛）。诱因是深色物件污染前景掩膜 → 质心偏移 →
     极坐标展开错位。这是 R8，也是下一步最该修的地方。

**本轮已修复的 R9**（贴边判据误报，详见 §7）：合计 51/80（63.7%）→ 64/80（80.0%），
噪点档 3/8 → 8/8，最接近真机手持的「组合·随手拍」档 0/8 → 8/8。

> ⚠️ **合成图通过 ≠ Gate 1 通过。** 合成图没有真实罗盘的印刷细节、材质反光、
> 手握遮挡与背景杂物。Gate 1 必须用真实照片跑 `photos` 子命令判定，当前**尚无真实样本**。
> 取证要求：≥5 类拍摄条件 × 各 ≥3 张，合计 ≥15 张（脚本缺样本时会打印指引）。

---

## 9. 下一步建议

> **当前进度（2026-09-16 23:00）**：Phase 0~3 全部落地（626 测全绿），移动端完整闭环，
> 容器编排就绪，端到端冒烟 34/34。**唯一未判定的是 Gate 1**，且它无法由我单方完成 —— 需要真实照片。

### 9.1 必须由人提供才能推进的一件事

**真实罗盘照片**。Gate 1 的判据是"普通用户拍一张真实罗盘照片能否稳定识别"，
合成图在原理上无法回答这个问题（缺印刷细节、材质反光、手握遮挡、背景杂物）。

取证要求（`scripts/gate1_eval.py photos` 会打印同样的指引）：

| 拍摄条件 | 张数 | 为什么需要 |
|---|---|---|
| 光线充足正面拍 | ≥3 | 基准上限 |
| 弱光 / 反光 | ≥3 | 实测合成反光档 0/8，是最硬的失效条件 |
| 斜拍（明显俯角） | ≥3 | 验证透视校正 |
| 盘体与背景同色系 | ≥3 | 验证前景掩膜选择 |
| 手指遮挡盘沿 | ≥3 | 真实拍摄几乎必然发生 |

放入 `real_photos/` 并从 `real_photos/labels.csv` 标注坐山/角度即可跑。
**不足 15 张时脚本会明确提示"成功率无统计意义"**，不会给一个虚假的达标结论。

### 9.2 不依赖真实照片、可以立刻做的

按性价比排序：

1. **修 R8（假阳性）** —— 掩膜抗污染。背景杂物档 1/8 与 0/8 是当前最低分，
   且属"给了错误答案却不自知"的高危类型。方向：选择前景掩膜时排除与盘体不连通的
   边缘连通域，或改用鲁棒质心。
2. **建立假阳性回归基线** —— 把当前 15/96 写成测试，防止修 R8 时先把指标做坏
   再"修好"。没有基线的优化无法判断是否真的变好。
3. **真机/模拟器 UI 走查** —— 移动端只验证了打包与类型，**未在设备上实走过一遍**。
   特别是「3 秒内确认」这个 Gate 1 的交互指标，只能在真机上测。
4. **反光档专项** —— 0/8 且原因是"未检测到鱼丝线"。若真实罗盘多为玻璃面，
   这一档的优先级要提到 R8 之前。

### 9.3 明确不建议现在做的

- **不要把 `needs_user_confirmation` 优化掉**。它是 R8 假阳性目前唯一的防线。
  在假阳性降到可接受水平之前移除确认页，等于把"错误但可见"变成"错误且静默"。
- **不要为了"编排完整"引入数据库/缓存/对象存储**。当前存储层是 SQLite 单文件，
  没有 Redis / S3 调用。要改先改代码，再改编排。

---

## 附录 A：实证记录（2026-09-16）

### A.1 环境

```
Python 3.13.12 (managed)
venv: C:\Users\anyong\.workbuddy\binaries\python\envs\default
lunar-python: 已安装并验证
sxtwl: ❌ 安装失败（Py3.13 无预编译 wheel，构建报错）
```

### A.2 实算输出

```text
公历输入 : 1981-09-14 08:00:00
农历     : 一九八一年八月十七

四柱     : 辛酉 丁酉 乙未 庚辰
日主     : 乙

纳音     : 石榴木 山下火 沙中金 白蜡金
年柱五行 : 金金
月柱五行 : 火金
日柱五行 : 木土
时柱五行 : 金土
生肖     : 鸡
```

### A.3 交叉验证

**路径一：lunar-python** → `乙未日 / 乙木 / 庚辰时`

**路径二：标准儒略日公式 + 恒定偏移**

| 日期 | JDN | 日柱 | 序号 | 偏移 k |
|---|---|---|---|---|
| 1981-09-14 | 2444862 | 乙未 | 31 | **49** |
| 2000-01-01 | 2451545 | 戊午 | 54 | **49** |
| 2024-02-10 | 2460351 | 甲辰 | 40 | **49** |
| 1990-05-20 | 2448032 | 乙酉 | 21 | **49** |

`k ≡ 49 (mod 60)` 在四个日期上**完全恒定** → lunar-python 采用标准 JDN 线性映射；且 `2000-01-01 = 戊午日` 为公认锚点 → 结果可信 `[已确认]`。

**路径三：纯干支规则自洽性（不依赖任何库）**

| | 日干 | 辰时应有 | 自洽 |
|---|---|---|---|
| 演示图 | 辛 | **壬辰** | ❌ 实为「甲辰」 |
| lunar-python | 乙 | **庚辰** | ✅ 实为「庚辰」 |

口诀依据：「丙辛从戊起」（辛日子时戊子）、「乙庚丙作初」（乙日子时丙子）。

### A.4 复现方式

```bash
PY="C:/Users/anyong/.workbuddy/binaries/python/envs/default/Scripts/python.exe"
"$PY" -c "
from lunar_python import Solar
ec = Solar.fromYmdHms(1981,9,14,8,0,0).getLunar().getEightChar()
print(ec.getYear(), ec.getMonth(), ec.getDay(), ec.getTime(), '| 日主:', ec.getDayGan())
"
```

---

## 附录 B：材料索引

| 材料 | 路径 | 用途 |
|---|---|---|
| 产品与架构方案 | `docs/xuanpan-ai-APP-project.md` | 权威来源：架构、功能矩阵、阶段划分、AGENTS RULE |
| UI/模型/网络规范 | `docs/玄盘 AI — UI、模型配置与网络线路设计规范.md` | 权威来源：UI 结构、模型抽象、网络策略 |
| 界面演示图 | `docs/ChatGPT Image 2026年9月16日 20_11_06.png` | **仅作视觉参考，数据不可采信**（§2.5） |
| 开发规范 | `AGENTS.md` | 行为约束，最高优先级 |
