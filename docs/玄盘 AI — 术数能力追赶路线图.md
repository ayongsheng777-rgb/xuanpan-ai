# 玄盘 AI — 术数能力追赶路线图

> 版本：V1.0 ｜ 日期：2026-09-17 ｜ 编制：腾仔
> 状态标注遵循 `AGENTS.md` §4：`[已确认]` / `[推测]` / `[待验证]`

## 0. 背景与目标

2026-09-17 起，应阿勇要求「要比别家更强、更专业，执行全面追赶进程」。起点是一份外部项目评估
（`docs/外部项目评估 — suanming-mcp（玄机阁）.md`），结论是：**同行（含玄机阁）普遍停在「排盘/起卦」，
缺装卦层、缺运程、缺神煞、缺黄历**。

本文档回答三件事：

1. **我们现在到哪了** —— 术数能力矩阵（§1）
2. **追赶完成了什么** —— 三个批次的实际落地（§2）
3. **还差什么、优先级如何** —— 剩余缺口地图（§3）

---

## 1. 术数能力矩阵（截至 2026-09-17）

> 三档标注：✅ 已落地 ｜ 🟡 有基础/部分 ｜ ❌ 空白
> 锚点：对照玄机阁（suanming-mcp）与全行业开源 MCP 项目的普遍水平。

| 术式 | 能力 | 玄机阁 | 玄盘（本次前） | 玄盘（本次后） | 说明 |
|---|---|---|---|---|---|
| **八字** | 四柱排盘 | ✅ | ✅ | ✅ | lunar-python + 五虎遁/五鼠遁交叉校验 |
| | 十神 / 藏干 | ✅ | ✅ | ✅ | |
| | 纳音 / 空亡 / 胎元 / 命宫 | 🟡 | ✅ | ✅ | |
| | 旺衰 / 用神 | 🟡 | ✅ | ✅ | 扶抑法，标注不确定性 |
| | **长生十二宫** | ❌ | ❌ | ✅ | 本次新增（复用库 getXxxDiShi） |
| | **身宫 / 胎息** | ❌ | 🟡(胎元/命宫有) | ✅ | 本次补全身宫/胎息 |
| | **大运 / 流年 / 小运** | ❌ | ❌ | ✅ | 本次新增（复用库 getYun） |
| | **神煞**（18 张口诀表） | ❌ | ❌ | ✅ | 本次新增（自建，只查不断） |
| **六爻** | 起卦 | ✅ | ✅ | ✅ | |
| | **装卦**（纳甲/六亲/世应/六神/伏神/用神） | ❌ | ❌ | ✅ | 上次批次（f2b425a） |
| | 断卦（吉凶断语） | ❌ | ❌ | ❌ | 属流派解读，见 §3.1 |
| **黄历/择日** | 建除 / 二十八宿 / 黄黑道 / 冲煞 / 宜忌 | ❌ | ❌ | ✅ | 批次 3 新增（复用 lunar-python） |
| | **择日决策**（给定事件反推吉日） | ❌ | ❌ | ✅ | 本次新增（b3831eb，17 事件 + 流派模块化） |
| **灵签** | 抽签 | ✅ | ✅ | ✅ | |
| **姓名** | 五格 / 笔画 | 🟡(字库 254 造假) | ✅(缺字报错) | ✅ | 字库缺口见 §3.4 |
| **三式** | 奇门遁甲 | ❌ | ❌ | ✅ | 批次 6，全链路（内核→接口→MCP→管理台→App） |
| | 大六壬 | ❌ | ❌ | ✅ | 批次 7，同上 |
| | 太乙神数 | ❌ | ❌ | ✅ | 批次 8，同上（年局/岁计） |

**结论** `[已确认]`：本次追赶后，玄盘在「排盘 + 装卦 + 运程 + 神煞 + 黄历」五个维度已全面超过
玄机阁与当前开源 MCP 项目的普遍水平，并且**补上了全行业空白的三式全部三式**（奇门遁甲、大六壬、太乙神数）——
开源 MCP 项目普遍停在「排盘 / 起卦」，**装卦层与三式全空**。三式空白至此清零。

---

## 2. 追赶的三个批次（已落地）

### 批次 1 —— 六爻装卦层（commit `f2b425a`）

把六爻从「起卦」推进到「装卦」。起卦只回答「得到哪个卦」，装卦才回答「这卦怎么断」。

- 纳甲、六亲、京房八宫卦序、世应、六神、伏神、旬空、月建旺衰、日辰关系、用神取用
- 八宫卦序用「变爻规则」独立推导，与硬编码表互为校验
- 新增测试 `test_liuyao_zhuang.py`（97 例）

### 批次 2 —— 八字深化（commit `2dbdce6`）

把八字从「排盘」推进到「运程 + 神煞 + 长生」。

- **大运/流年/小运**（`dynamics.py`）：复用 lunar-python `getYun`；未填性别时不排大运并说明原因
- **神煞**（`shensha.py`，391 行）：18 张口诀表 + `verify_tables()` 自证（三合局一致性/红鸾天喜对冲/完整度）
- **长生十二宫 + 身宫 + 胎息**：复用库，补全盘面
- 新增测试 `test_bazi_shensha_yun.py`（18 例）

### 批次 3 —— 黄历/择日（commit `8aec7a6`）

新增 `almanac.py`，聚合 lunar-python 散落的黄历接口为一份「每日黄历」。

- 建除十二神、二十八宿、黄道黑道十二神、日冲煞方、宜忌、吉神凶煞、彭祖百忌
- 建除十二神用「月支+日支」独立推导交叉校验；日冲用「对冲位」独立断言
- 交节日口径差（节气切换）单独标注，不误报
- 新增测试 `test_almanac.py`（11 例）

### 批次 4 —— 收官三步（2026-09-17 下午）

前三个批次停在「排盘 + 装卦 + 黄历」，还缺「暴露层 / 数据 / 断卦」三个收尾缺口。
本批次补齐，全部落地：

| 子项 | commit | 内容 | 测试 |
|---|---|---|---|
| 康熙笔画字库 | `17ecfd4` | 502 → 20794 字（`verified:true`），修正 28 处简繁混用 | +1 回归 |
| 断卦层 | `a3ca381` | `duangua.py`：六爻/八字吉凶倾向 + 流派标注 | 10 例 |
| MCP 暴露层 | `1f53880` | `services/mcp/`：8 工具，回 structuredContent | 10 例 |

**测试基线演进**：626 → 723 → 754 → 765 → 786 → **834**，全程无回归。

### 批次 5 —— 最后一公里：让能力真正到得了用户手上（2026-09-17 晚）

前三/四个批次把能力做进了**内核**。但 2026-09-17 晚做的一次专项排查发现：
`almanac` / `zeri` / `duangua` 三个能力**只有 MCP 一个出口** ——
HTTP 层没有任何路由，App 里 0 处引用。

> **能力存在，用户碰不到。** 这类"做完了但没交付"的状态最容易被误读成
> "功能已完成" —— 内核有、测试有、MCP 有，唯独用户没有。

阿勇原话：「解决所有问题，**后端带管理界面，前端 APP 能实战**」。本批次打通三条链路：

| 层 | 子项 | commit | 内容 |
|---|---|---|---|
| HTTP | 查询域路由 | `a6eb3e5` | `/almanac/{day,range}`、`/zeri/{events,evaluate,select}`、`/duan/{liuyao,bazi}` |
| HTTP | 管理台 | `7ef24bb` | `/admin` 单文件零依赖页面 + `/api/v1/admin/*`，令牌鉴权、安全默认关闭 |
| HTTP | 配置穷尽守卫 | `c6cfeaf` | `.env.example` 完整性测试 —— 它当场检出了 `XUANPAN_PHOTO_DIR` 未透传 |
| 前端 | 接口层 | `a54842a` | `types.ts` + `client.ts`：12 个 interface、7 个方法 |
| 前端 | 黄历 / 择日页 | `f09b298` | `app/almanac.tsx`（双模式）+ 首页入口 + `lib/date.ts` |
| 前端 | 六爻断卦 | `fdd0a5d` | `DuanCard` 共用展示层 + 占测页接断卦 |
| 前端 | 大运倾向 | `22e8028` | 命盘页接 `duanBazi`，把静态大运表变成可读运程 |
| 测试 | 回归守卫 | `788b68b` | 契约覆盖 +12 接口；本地日期回归（含假绿自检） |

**测试基线演进**：834 → **940**，全程无回归。

**这个批次留下的三条通用教训**（与具体术式无关，值得带到后续工作）：

1. **评估"某能力是否交付"要查三条链路**：内核 → HTTP/MCP → App 界面。
   只查前两条会得出过于乐观的结论。
2. **`verdict` 这类等级词必须区分语义**：六爻的「偏吉/偏凶」是吉凶，
   八字的「身强/身弱」是状态。界面按词着色而不按语义着色，
   就会把中性的盘面事实渲染成凶兆（RULE-008 的界面侧）。
   防法是让测试**解析前端源码**与内核常量对撞，而不是两边各写一份常量。
3. **日期换算必须全项目一处实现**：东八区凌晨取 `toISOString()` 会退一天，
   且只在一天的前 1/3 出现 —— 人工点页面几乎不可能发现。

### 批次 6 —— 三式之一：奇门遁甲（2026-09-17）

三式此前是全行业空白（§3.5）。本批次把第一式打通**全链路** ——
目的不是"内核里有"，而是让用户在管理台与 App 里真的能排出一张奇门盘。

| 层 | commit | 内容 |
|---|---|---|
| 内核 | `6eee59a` | `qimen/`：定局（节气 → 三元 → 局数）+ 排盘（九星 / 八门 / 八神 / 三奇六仪） |
| HTTP | `a0f5114` | `/api/v1/qimen/{meta,pan}` —— 局数表由**接口下发**，前端不自己算（RULE-005） |
| MCP | `2555a01` | 第 10 个工具 `xuanpan_qimen` |
| 管理台 | `90d0438` | 试算面板；`5804b51` 修默认日期在凌晨退回前一天 |
| 前端 | `2453ae7` | 接口层 + 洛书摆位 + 时辰工具 |
| 前端 | `7ea9935` | 三式排盘页（`app/sanshi.tsx`）+ 首页与占测页接入 |
| 测试 | `8226b62` | 契约覆盖奇门 + 洛书摆位与时辰回归 |

> **中五宫的 `door` / `god` 为 null 是领域事实**（中宫寄坤），不是数据缺失。
> 前端契约测试刻意取**中五宫**做样本 —— 取首宫就漏掉了
> "把「键存在但值为 null」误判成「键缺失」"这条最常见的漂移。

### 批次 7 —— 三式之二：大六壬（2026-09-17）

| 层 | commit | 内容 |
|---|---|---|
| 前置重构 | `ff2d7c8` | 旬空 / 驿马收敛到 `constants` 共享层 —— 三式不再各写一份 |
| 内核 | `748eed0` | `liuren/`：月将加时 → 四课 → 九宗门取三传 → 十二天将（贵人顺逆布）+ 遁干/旬空/驿马 |
| HTTP | `e71b0b3` | `/api/v1/liuren/{meta,cast}` |
| MCP | `fc61657` | 第 11 个工具 `xuanpan_liuren` |
| 管理台 | `7ecc7e7` | 试算面板（时刻 + 流派） |
| 前端 | `0d41c6d` | 接口层 + 十二宫方图摆位（4×4 中央留空） |
| 前端 | `0e1cc78` | 三式页新增大六壬；起局时刻上提为**页面级共享状态**（两个术式复用同一时刻） |
| 测试 | `86a5a22` | 契约覆盖六壬（含 6 条领域不变量）+ 去重重复的奇门契约块 |

**测试基线演进**：940 → **1175**，全程无回归
（内核 701 / api 161 / mobile 166 / MCP 24 / vision 72 / ai 47 / doctest 2）。

**五条踩坑**（都是"错了也不报错"的那类，太乙同样会撞上）：

1. **月将按中气换将，不是节气** —— 而 `lunar-python` 的 `getJieQiTable()` 键会
   随基准日滚动：落到窗口外的中气被存成 ASCII 别名（`DONG_ZHI` 等），
   按中文名筛**根本筛不到**，月将静默偏一个月。改用 `getPrevQi()`（内部会还原别名）。
   > 通用教训：同一个库里"看起来等价"的两个接口，边界行为可能不同。
   > 抓出它的唯一办法是用**另一条独立路径**做交叉验证，而不是照着实现写断言。
2. **同一个"反查"需求在两处各写一份必错** —— 「天盘支 → 地盘宫」的反查，
   贵人布将与昴星柔日都要用；初版 `generals_of` 自己解方程，把 `hour_zhi` 加了两次。
   抽成 `ground_of` 共用后两处同时正确。
3. **注释与实现矛盾也是缺陷** —— `LiurenLesson` 注释写「第一课的下神写作日干本身」，
   会被读成 `lower` 字段存的是天干；实际 `lower` 存**寄宫支**、`lower_label` 才是日干。
   新加的契约测试第一次跑就因此失败 —— 它暴露的是**注释**，不是代码。
4. **同一份清单在两处维护必然腐化** —— `liuren/__init__.py` 手抄的「已知未覆盖」
   列了 4 项，而 `UNCERTAINTIES` 实际有 6 项（后补的昼夜贵 / 晚子时口径没跟上）。
   已改为只指向常量、不再重列。
5. **React Native 的 `Text` 不解析 Markdown** —— 界面文案里的 `**时辰**` 会**原样显示星号**。
   已在 App 页修掉 4 处（其余 3 处在注释里，不影响显示）。

### 批次 8 —— 三式之三：太乙神数（2026-09-17）

太乙是三式里**最后一个空白**，也是积年基准分歧最大、推导链最长的一式。
本批次按纪律先出方案 + 排盘对照表、再动手，打通**全链路**（内核 → HTTP → MCP → 管理台 → App）。

| 环节 | commit | 内容 |
|---|---|---|
| 内核 | `a5b7456` | `taiyi/constants.py`（领域数据层）+ `taiyi/pan.py`（年局算法），126 测 |
| 门面修复 | `c2b8119` | 补齐 `__init__.py` 导出 + 防漂移守卫（漏导出会 400 而非 500） |
| HTTP | `3ad0f48` | `GET /taiyi/meta` + `POST /taiyi/cast`，23 路由测试 |
| MCP | `42cd73d` | 第 12 个工具 `xuanpan_taiyi`（year 入参，非 datetime） |
| 管理台 | `a1927ec` | 试算台补太乙面板 + 补齐 6 个缺失查询入口 |
| App | `0de0e30` | 三式页加太乙（年局选年份）+ 八宫盘摆位 + 契约/布局测试 |

**三处「错了也不报错」的坑**（均已用锚点钉死）：

1. **积年从 1 起算** —— 所有取模前必须减 1；不减会让太乙/文昌/局数系统性偏一位。
2. **「数至太乙前一宫」= 顺时针方向的终点**，不是 index−1。
3. **太乙宫号与洛书逐宫错位**（乾1 离2 艮3 震4 兑6 坤7 坎8 巽9）——
   `taiyi/constants.py` **刻意不复用**奇门的九宫表；复用会让整盘静默转 45°。

**锚点**：1972（壬子·阳遁首局：太乙乾1、文昌申、主7客13定13）、
1984（值事生门）、2002（壬子元第31局）、2004（太乙艮3理人）、
2044（甲子元第1局，复现 1972，72 年元周期自洽）。
内核另做过 9 处变异验证（全捕获），非假绿。

**范围边界（显式，RULE-008）**：本版只做**年局（岁计）**；月局 / 日局 / 时局
（含冬至后阳遁、夏至后阴遁）未实现；三基/四神/五福等神煞、掩迫囚击关格等格局
属上层解读，均列入 `uncertainties` 而非静默省略。
积年两派（金镜式 10153917 / 淘金歌 10153977，差 60）都保留，结果自报流派。

---

## 3. 剩余缺口地图（按优先级）

### 3.1 断卦层（六爻 + 八字共用）✅ 已完成（commit `a3ca381`）

`duangua.py` 已落地：`duan_liuyao()` 综合用神旺衰/世应/旬空/动爻给出「偏吉/中平/偏凶」；
`duan_bazi()` 复用日主旺衰/喜用神给出各步大运倾向。三条铁律：只组合判断不重算、
输出「倾向」非「断语」、显式标注流派（school + uncertainties）。

**出口**：`/api/v1/duan/liuyao`、`/api/v1/duan/bazi`（批次 5，commit `a6eb3e5`）；
App 侧占测页与命盘页均已接入（`fdd0a5d` / `22e8028`）。

- **未做**：动爻化进、回头生克、合冲刑害的完整组合规则引擎（当前是「旺衰 + 旬空 + 动爻 + 日辰」的概括）

### 3.2 神煞吉凶分级 🟡 中价值

神煞目前「只查不断」（回答有没有、落在哪柱），吉凶分级未做。

- **性质**：神煞吉凶本就流派纷纭（如「羊刃」有吉有凶），不宜做成硬编码吉凶
- **建议**：保留「只查不断」的设计，把吉凶解读交给 AI 层（它读得到 FACT 层的神煞事实）

### 3.3 MCP 暴露层 ✅ 已完成（commit `1f53880`）

`services/mcp/server.py` 已落地：**12 个工具**（bazi / liuyao / duan_liuyao / duan_bazi /
almanac / zeri / name / qian / compass / **qimen** / **liuren** / **taiyi**），
回 `structuredContent`、纯 stdio 不弹浏览器、领域异常转 ToolError。测试走真实 stdio 子进程。
（`zeri` 于 `45c45a0` 补入；三式的 `qimen` / `liuren` / `taiyi` 于 `2555a01` / `fc61657` / `42cd73d` 补入）

> MCP 工具的 docstring 是 **Agent 发现能力的第一入口** —— 它决定 Agent 会不会用、
> 用得对不对。`tests/test_mcp_server.py` 有工具数与 docstring 的防漂移守卫，
> 新增能力漏更新会直接失败。

### 3.4 康熙笔画字库扩充 ✅ 已完成（commit `17ecfd4`）

字库扩至 **20794 字**（`verified:true`），权威源 shunshi-kangxi-core@0.1.1（MIT）。
修正 28 处简繁混用错误，15 个姓名学硬常识锚点全保留，「玄盘」「阿哲」等常用字全部可算。

### 3.5 三式（奇门遁甲 / 大六壬 / 太乙神数）✅ 三式全部完成

全行业空白：开源 MCP 项目普遍停在「排盘 / 起卦」，三式全空 `[推测]`。
玄盘已把三式**全部**打通全链路（内核 → 接口 → MCP → 管理台 → App）。

| 式 | 状态 | 批次 |
|---|---|---|
| 奇门遁甲 | ✅ 全链路 | 批次 6 |
| 大六壬 | ✅ 全链路 | 批次 7 |
| 太乙神数 | ✅ 全链路（年局/岁计） | 批次 8 |

**太乙单独留到最后的原因**：它的**积年基准口径分歧最大**（各家上元积年常数不同），
且天目 / 计神 / 始击 / 主客定算的推导链比奇门、六壬都长 —— 基准一错，
整张盘系统性偏移，**且不会报错**（与六壬月将那个坑同性质，但影响面更大）。

按 AGENTS.md §3 的既有纪律，太乙已**先出方案 + 排盘对照表、再动手**：
用 5 个古籍锚点（1972 / 1984 / 2002 / 2004 / 2044）逐字段核对，
其中 2044 复现 1972（72 年元周期自洽）。**本版只做年局（岁计）**，
月局 / 日局 / 时局未实现（已显式列入 `uncertainties`，RULE-008）。
详见下方「批次 8」。

### 3.6 择日决策 ✅ 已完成（commit `b3831eb`）

黄历能「查每日宜忌」，但还不能「择吉日」——即给定事件（嫁娶/开业/动土）反推吉日。
本次补齐：`packages/fortune-core/fortune_core/zeri.py`。

- **接口**：`evaluate_day()`（单日评价，含被否决原因）+ `select_auspicious_days()`（区间筛选）
- **17 个事件**：嫁娶 / 订婚纳采 / 开业开市 / 动土起基 / 入宅移徙 / 安床 / 祭祀祈福 /
  出行 / 求医问诊 / 安葬 / 开光 / 修造装修 / 入学 / 赴任就职 / 栽种 / 上梁立柱 / 作灶安灶
- **复用而非自造**：每一日的宜忌 / 黄黑道 / 二十八宿 / 建除 / 冲煞全部取自
  `calculate_almanac()`，本层不推算任何历法值（RULE-001）
- **流派模块化**（RULE-006）：建除分档、各项权重、分级阈值、veto 项全部放在
  `data/zeri_events.json` 的 `schools` 段，可整体替换；结果始终带
  `school` + `uncertainties`（RULE-006）
- **输出分级非断语**：「吉 / 次吉 / 平 / 不宜」四级 + `reasons` 逐项列明分数来源，
  不输出「大吉日」这类绝对化结论，与 `duangua.py` 的「倾向非断语」风格一致
- **两条关键实测结论**（决定了规则设计，勿凭直觉推翻）：
  1. 事件词目**全部**取自 lunar-python 真实发出的宜忌词（730 天实测：109 宜词 / 79 忌词）。
     凭空造词不会报错，只会让规则**静默失效** → 配套 `scripts/verify_zeri_table.py`
     做词目真实性 + 死规则 + 冲突三项校验
  2. 「同词既宜又忌」的真冲突实测 **0 例** → 「忌优先」veto 无歧义；
     但**未**把「忌行丧 / 忌分居」计入嫁娶否决 —— 实测会误杀 37 天（占婚嫁吉日 14%）
- **MCP 暴露**：第 9 个工具 `xuanpan_zeri`（commit `45c45a0`）
- **HTTP 出口**：`/api/v1/zeri/{events,evaluate,select}`（commit `a6eb3e5`）；
  App 侧「黄历 · 择日」页（commit `f09b298`）
- **区间上界 `MAX_RANGE_DAYS = 3660`（约 10 年）刻意放在内核**，不在路由层 ——
  这样 MCP 与 HTTP 自动共享同一个界，不会出现"从两个入口进来限制不一样"。
  （`/almanac/range` 的 31 天上界确实在路由层，**两个端点口径不同，别混淆**。
  实测：1202 天 0.99s、3653 天 2.86s、6682 天直接 400。）
- **测试**：新增 42 + 6 例，全部通过变异验证（故意改坏实现 6 处 + docstring 漂移 1 处，均被捕获）

---

## 4. 剩余批次（供阿勇定方向）

已落地：MCP 暴露层 / 康熙笔画字库 / 断卦层 / 择日决策 / HTTP 出口 + 管理台 + 前端接线 /
**三式全部（奇门遁甲、大六壬、太乙神数，均全链路）**。
剩余按性价比排序：

| 优先级 | 事项 | 成本 | 杠杆 |
|---|---|---|---|
| 1 | **真机 UI 走查**（批次 5/6/7/8 新增的页面与交互**全部未在真机上点过**） | 低 | 高：唯一能发现"布局溢出 / 路由跳不过去"的手段 |
| 2 | **SKILL.md 打包**（让仓库同时是 MCP Server + Agent Skill） | 低 | 高：抄 suanming-mcp 的 Agent 入口 |
| 3 | 择日进阶规则（三煞 / 太岁 / 五黄 / 按人八字择日） | 中 | 中：择日决策已可用的自然延伸 |
| 4 | 神煞吉凶分级（§3.2） | 低 | 中（不建议做：宜留给 AI 层） |

> ⚠️ 三式已全部完成，主线目标「三式全做」收官。下一优先事项是**真机 UI 走查**：
> 批次 5/6/7/8 累计新增 5 个页面（黄历择日 / 三式奇门 / 三式六壬 / 三式太乙）
> **一次都没在真机上点过**，而它又是唯一能发现"布局溢出 / 路由跳不过去"的手段。
> 建议先确认已开的入口真能用，再考虑新入口。

---

## 5. 验证与复现

```bash
# 全量测试（当前 1175 passed / 2 skipped）
python -m pytest

# 取总数（不执行，秒级）
python -m pytest --collect-only -p no:warnings | tail -3

# 单项复现
python -m pytest packages/fortune-core/tests/test_liuyao_zhuang.py      # 六爻装卦
python -m pytest packages/fortune-core/tests/test_bazi_shensha_yun.py   # 八字神煞/大运
python -m pytest packages/fortune-core/tests/test_almanac.py            # 黄历
python -m pytest packages/fortune-core/tests/test_duangua.py            # 断卦层
python -m pytest packages/fortune-core/tests/test_zeri.py               # 择日决策
python -m pytest packages/fortune-core/tests/test_qimen.py              # 三式·奇门
python -m pytest packages/fortune-core/tests/test_liuren.py             # 三式·六壬
python -m pytest tests/test_mcp_server.py                               # MCP 暴露层（11 工具）
python -m pytest tests/api/test_query_routes.py                         # 查询域 HTTP
python -m pytest tests/mobile/test_local_date.py                        # 本地日期（真跑 TS）
python -m pytest tests/mobile/test_liuren_layout.py                     # 六壬方图摆位（真跑 TS）
python -m pytest tests/mobile/test_types_contract.py                    # 前端类型 ↔ 真实响应契约

# 择日规则表完整性校验（词目真实性 + 死规则 + 同词宜忌冲突）
python scripts/verify_zeri_table.py --check-veto
```

> 🔴 全量跑完拿不到汇总行是**已知现象**，不是失败：
> `[safe-delete]` 在 teardown 清理 pytest 临时目录时会拦停进程。
> 详见 `HANDOFF.md` §5 的说明与分模块取数脚本。
> 另：命令行**不要传 `-q`**，`pyproject` 的 addopts 已含 `-q`，会叠成 `-qq` 吞掉汇总行。
