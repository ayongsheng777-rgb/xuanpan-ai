# AGENTS.md

> 本文件是本仓库的**最高开发规范**。
> 任何 AI 在新会话中接手本项目时，**必须先完整阅读本文件，再执行任何读取、修改、运行动作**。

---

## 0. 会话启动检查（新会话第一步）

在回应任何任务之前，先完成以下动作：

1. 完整读取本文件
2. 执行 `git status` 与 `git log --oneline -10`：确认工作区是否干净、最近改动是什么
3. 如存在 `.workbuddy/memory/`，读取最近的工作日志
4. 若工作区存在**未提交改动**，先向用户确认其来源，再决定是否在其基础上继续

> 这条的意义：AI 没有跨会话记忆。**仓库状态与 git 历史就是唯一的记忆载体**。不先读状态就动手，等于闭着眼睛改代码。

---

## 1. 项目

- **名称**：玄盘 AI（XuanPan AI）
- **定位**：罗盘视觉识别 × 确定性术数计算 × AI 解释的移动端应用。核心不是「AI 算命」，而是**把实体罗盘转成结构化数字数据**，再用确定性计算与 AI 语言能力完成现代化交互
- **核心工程原则**：**AI 负责看 / 理解 / 解释；代码负责算 / 校验 / 映射**
- **技术栈**：已定方向，见 §5

### 1.1 方案材料索引

| 材料 | 路径 | 效力 |
|---|---|---|
| 产品与架构方案 | `docs/xuanpan-ai-APP-project.md` | 权威（架构 / 功能矩阵 / 阶段） |
| UI、模型配置与网络线路设计规范 | `docs/玄盘 AI — UI、模型配置与网络线路设计规范.md` | 权威（UI / 模型 / 网络） |
| 界面演示图 | `docs/ChatGPT Image 2026年9月16日 20_11_06.png` | **仅作视觉参考，其中术数数据不可采信** |
| **产品基线规范** | `docs/玄盘 AI — 产品基线规范.md` | ⭐ **唯一真源**：已裁定项一律以它为准 |
| 生产路线报告 | `docs/玄盘 AI — 生产路线报告.md` | 冲突裁定与实施路线 |

> ⚠️ 三份原始材料存在 **5 处内部不一致**：其中 **4 项已裁决**（底部导航 / AI 报告标签 / 配色 / 相机入口），结论固化于《产品基线规范》；**隐私文案 1 项仍待处理**。
> **遇到材料冲突时，一律以《产品基线规范》为准**；该文件未覆盖的冲突，先回报确认，不得自行择一执行。

---

## 2. 最高约束（不可违背）

以下两条为本项目铁律。任何优化、重构、"临时方案"都不得绕过它们。

### 2.1 每次改动必须创建对应的 git commit

- **一次改动完成 = 一个可提交点**。不允许把多次改动攒在一起提交
- 每个 commit 只承载**一个逻辑单元**（一个功能 / 一次修复 / 一次文档更新）
- 严禁在同一个 commit 中混入无关改动
- 提交信息格式：

```
<type>(<scope>): <简述>

<可选正文：为什么这么改>
```

`type` 取值：

| type | 含义 |
|---|---|
| `feat` | 新功能 |
| `fix` | 修复缺陷 |
| `docs` | 文档变更 |
| `refactor` | 重构（不改行为） |
| `test` | 测试相关 |
| `chore` | 杂项、依赖、配置 |
| `perf` | 性能优化 |

- 提交前必须 `git status` 复核暂存内容。**禁止 `git add -A` 盲目全加**，避免误提交密钥、`.env`、构建产物

### 2.2 每次改动必须同步测试，交付前测试与验收全部通过

- **新增功能 → 必须新增对应测试**
- **修改已有行为 → 必须更新受影响的测试**
- **「代码写完」不等于完成**。只有测试实际运行且通过，才算完成
- 交付回复中必须给出：**实际执行的测试命令 + 真实输出摘要**
- **严禁**在未实际运行的情况下声称"测试通过"
- 客观上无法验证的内容（如依赖外部服务、无测试环境的场景），必须显式标注 `[未验证]` 并说明原因

> 这条的意义：AI 倾向于用"看起来对"代替"确实跑通"。**要求给出真实命令输出，是唯一能穿透这种自我欺骗的机制。**

### 2.3 术数领域专属铁律（RULE-001 ~ RULE-010）

以下 10 条来自产品方案 §43，是**不可违背**的领域约束：

| 编号 | 规则 |
|---|---|
| RULE-001 | 确定性计算必须由代码完成 |
| RULE-002 | LLM 不得修改计算层输出 |
| RULE-003 | 视觉模型无法确认时必须返回 null |
| RULE-004 | 所有 AI 识别结果必须允许用户修正 |
| RULE-005 | 罗盘规则不得散落在 UI |
| RULE-006 | 流派规则必须模块化 |
| RULE-007 | 所有核心算法必须有单元测试 |
| RULE-008 | 不得为了"看起来合理"而修正用户数据 |
| RULE-009 | 任何领域规则变更必须同步更新测试 |
| RULE-010 | 医疗、投资、法律等问题不得伪装成命理确定性结论 |

> **实测佐证**（2026-09-16）：对界面演示图 4 的八字数据做代码实算，发现其**日柱、时柱、日主三项**与真实排盘不符（详见生产路线报告 §2.5）。
> **人工编造的术数数据，会以肉眼无法识别的方式出错** —— 这正是 RULE-001 存在的理由。

---

## 3. 工作流程

按下述链路推进，不得跳步：

```
需求 → 现状 → 方案 → 实施 → 验证 → 交付
```

- **需求**：先理解真实目标。口语化需求必须转译为「真实目标 → 功能拆解 → 技术方案 → 验证标准」
- **现状**：先看代码、配置、日志、环境。**禁止凭空推测**
- **方案**：涉及架构选型、新增依赖、不可逆操作时，先给方案再动手
- **实施**：最小侵入。优先就地修复与复用现有逻辑
- **验证**：实际运行、构建、测试
- **交付**：按 §7 格式汇报

---

## 4. 状态标注

所有结论必须显式标注可信度，**禁止把推测当事实陈述**：

| 标注 | 含义 |
|---|---|
| `[已确认]` | 已通过代码 / 命令 / 日志实际验证 |
| `[推测]` | 基于经验的合理推断，尚未验证 |
| `[待验证]` | 需用户确认或环境验证 |

---

## 5. 技术栈（2026-09-16 回填）

| 层 | 选型 | 状态 |
|---|---|---|
| Mobile | React Native + Expo（重相机能力时 + VisionCamera） | 方案已定 |
| Backend | Python FastAPI | 方案已定 |
| 计算内核 | 自研 `packages/fortune-core`（Python） | 方案已定 |
| 历法引擎 | `lunar-python`（**已验证可用**）；`sxtwl` 在 Python 3.13 无预编译 wheel，构建失败 | lunar-python 可用 |
| 数据库 | PostgreSQL | 方案已定 |
| 缓存 | Redis | 方案已定 |
| 对象存储 | S3 / MinIO | 方案已定 |
| 模型接入 | Provider 抽象，**按能力而非品牌划分**（Vision / Reasoning / Fast / Fallback） | 方案已定 |

### 5.1 目录结构（monorepo）

```text
xuanpan-ai/
├── apps/mobile/              # React Native + Expo
├── services/                 # api / ai-router / vision / gateway / worker
├── packages/fortune-core/    # 唯一计算内核（确定性，禁 LLM 介入）
├── mcp/xuanpan-mcp/          # 后续：对外暴露计算能力
├── data/                     # 领域数据（二十四山 / 签文 / 命名）
├── tests/
├── docs/
└── AGENTS.md
```

### 5.2 环境基线

- Python：3.13.12（managed）
- venv：`C:\Users\anyong\.workbuddy\binaries\python\envs\default`（已建）
- 已安装并验证：`lunar-python`
- **已知坑**：`sxtwl` 需 C 编译环境，Py3.13 下安装失败；如必须使用，需降级 Python 版本

### 5.3 命令（已回填）

> 全部命令在项目根目录执行。`$PY` = `C:/Users/anyong/.workbuddy/binaries/python/envs/default/Scripts/python.exe`（managed venv）。
> 凡声称"测试通过"，必须附实际执行的命令与输出，不得引用本节占位。

**测试（无需安装，`conftest.py` 注入四个包的路径）**

```bash
# 全量
"$PY" -m pytest

# 分层
"$PY" -m pytest packages/fortune-core/tests   # 计算内核
"$PY" -m pytest tests/vision                  # 罗盘识别
"$PY" -m pytest tests/ai                      # AI 解释层
"$PY" -m pytest tests/api                     # 后端服务
"$PY" -m pytest tests/mobile                  # 前端几何 ↔ 后端山表 一致性

# 注意：pyproject 的 addopts 已含 -q，命令行再传 -q 会变成 -qq 而**吞掉汇总行**。
# 想看到 "N passed" 就不要重复传 -q；屏蔽依赖告警用 -p no:warnings。
```

**起服务**

```bash
# [Host] 注意 PYTHONPATH 用 Windows 分隔符 ';'（Git Bash 下用 ':' 会 import 失败）
PYTHONPATH='services/api;services/ai;services/vision;packages/fortune-core' \
  "$PY" -m uvicorn xuanpan_api.app:create_app --factory --host 127.0.0.1 --port 8360
```

> ⚠️ **8352 已被本机 SysCenter 占用**，本地联调一律用 8360 等其它端口。
> 文档：<http://127.0.0.1:8360/docs>

**移动端（`$NODE` = `C:/Users/anyong/.workbuddy/binaries/node/versions/22.22.2-3/node.exe`）**

```bash
# [Host] 安装依赖（首次）。registry 已在 apps/mobile/.npmrc 指向国内镜像 ——
# 本机 registry.npmjs.org 不可达（curl 返回 000），用默认源会**静默卡死**十几分钟
# 且 node_modules 一直不出现，很容易被误判成"Expo 依赖本来就大"。
cd apps/mobile && npm install

# [Host] 类型检查（零容忍）
cd apps/mobile && npm run typecheck

# [Host] 打包验证 —— 最强的一道：真实解析全部 import 与依赖图
# 产出 .hbc 字节码即为通过，不需要设备/模拟器。
# 必跑的理由：tsc 只查类型，不查模块能否解析 —— 本轮 query-string 缺失
# 就是"tsc 全绿、一打包就炸"。
cd apps/mobile && npx expo export --platform android --output-dir /tmp/xp-export

# [Host] 导出环形选择器几何（JSON 到 stdout），人工核对用
cd apps/mobile && npm run geometry:probe

# [Host] 起开发服务器（需先起后端）
cd apps/mobile && npm start
```

> `tests/mobile/` 下有两道前后端一致性校验，都**不依赖 React Native 运行时**：
>
> | 文件 | 校验什么 | 失败意味着 |
> |---|---|---|
> | `test_ring24_parity.py` | 真正执行 `apps/mobile/src/lib/ring24.ts`（Node ≥ 22.6 的 `--experimental-strip-types`），把几何结果与 `fortune_core.mountain24` 逐位比对 | 环形选择器与后端山表漂移 → 用户点「午」算出「未」 |
> | `test_types_contract.py` | 跑通真实链路取回响应，核对 `types.ts` 声明的字段是否都存在 | 前端字段名写错 → 界面某格静默变空（不报编译错） |
>
> 两者都做过**变异验证**（故意改坏后确认能失败），不是假绿。
> 没装 Node 时前者跳过而非失败。

**端到端冒烟（对真实服务发请求，非 TestClient）**

```bash
"$PY" scripts/smoke_api.py --base http://127.0.0.1:8360
```

**Gate 1 识别可用性评测**（生产路线报告 §5 的关键闸门）

```bash
# 合成退化扫描：12 档条件 × 8 真值角 = 96 例，定位算法从哪档开始失效。
# ⚠️ 它不能代替 Gate 1 —— 合成图没有真实罗盘的印刷细节/材质反光/手握遮挡。
"$PY" scripts/gate1_eval.py synthetic --out "docs/玄盘 AI — Gate1 合成退化扫描.md"

# 真实照片评测：Gate 1 的**正式判据**。缺照片时返回 2 并打印取证指引。
"$PY" scripts/gate1_eval.py photos --dir real_photos --out "docs/玄盘 AI — Gate1 真实照片评测.md"
```

> 看结果时**必须把「明确拒绝」与「假阳性」分开**：
>
> | 失败类型 | 表现 | 危害 |
> |---|---|---|
> | 明确拒绝 | `compass_detected=False` + 可操作提示 | 低（符合 RULE-003，用户知道要重拍） |
> | **假阳性** | 返回了山名候选，但山名是错的 | **高** —— 用户看到的是一个看似正常的错误坐向 |
>
> 报告里的「假阳性」单列一行就是这个原因；把它并进识别率会让最危险的失败模式隐形。
> 当前唯一防线是 `needs_user_confirmation=True`（RULE-004）。**这条防线不得为了「降低确认率」而被移除。**
>
> 报告由脚本生成，**不要手工编辑** —— 手改的数字无法追溯，改数据后重跑。

**数据表再生成**（`kangxi_strokes.json` 由脚本产出，防止手写出重复键与错值）

```bash
"$PY" scripts/gen_strokes_table.py --check     # 只校验，不写盘
"$PY" scripts/gen_strokes_table.py             # 重新生成
```

### 5.4 依赖引入的前置说明（持续有效）

引入任何新框架、依赖、工具链之前，必须先说明：

1. 为什么用它
2. 有没有更轻量的替代方案
3. 成本（**是否收费 / 是否需自备 API key / 是否需注册外部账号**）
4. 对现有代码的影响

取得确认后再引入。**Vision 与 LLM 调用是本项目最大成本项，任何新增计费依赖都必须先说明。**

### 5.5 禁止事项

- **禁止对技术栈做超出本节的假设**（不预设未列出的框架或目录）
- **禁止 API Key 出现在客户端**：不进 APK、不写死在 JS、不进 Git、不返回前端、日志脱敏、数据库加密存储

---

## 6. 红线

- **不可逆操作**（删除文件或数据、修改数据库、改系统配置、格式化磁盘）必须先备份、给出回滚方案，经确认后执行
- **禁止引入付费或需自备 API key 的依赖而不事先告知成本**。成本不明确或需付费时，先说明，由用户决定
- **禁止擅自重构、更换框架、删除既有功能**。确有必要时先提方案
- **禁止提交任何密钥、Token、密码、`.env`**。示例配置一律用 `.env.example`
- **禁止伪造能力与测试结果**。做不到就说做不到，并说明已完成部分、阻塞点、下一步

---

## 7. 交付格式

每次交付必须包含以下四段：

```
【完成内容】做了什么
【修改文件】文件路径 + 改动要点
【验证结果】实际执行的命令 + 真实输出；未验证项须标注
【剩余问题及下一步】遗留问题、风险、后续建议
```

---

## 8. 目录与产物约定

- 代码、测试、文档分目录存放，不混放
- 构建产物、依赖目录、本地配置一律**不提交**（落地为 `.gitignore`）
- `.workbuddy/` 为 AI 工作区（记忆、配置）；其中的 `memory/` 属于项目上下文，应提交

---

## 9. 本文件的维护

本文件是**活文档**，不是一次性产物：

- 技术栈确定后 → **必须回填 §5**
- 新增约定 → 追加到对应章节，并写明原因（为什么加这条）
- **修改本文件本身，同样必须遵守 §2 的两条约束**（独立 commit + 自检）

---

## 附：本规范的自我验证

本文件是否被真正遵守，可用以下方式检验：

```bash
git log --oneline          # 是否每次改动都有对应 commit
git status                 # 交付时工作区是否干净
```

若交付后 `git status` 仍显示未提交改动，则说明 §2.1 未被遵守。
