# 行尾与文本规范化 —— 统一跨平台行为
#
# 为什么需要这个文件：
#   Windows 上 core.autocrlf 默认 true，会把入库的 LF 转成工作区的 CRLF。
#   本项目是 Python + React Native 混合仓库，两侧对行尾的敏感度不同：
#   - Python 包内若混入 CRLF，某些工具链/tooling 会报错或产生无意义 diff
#   - RN/JS 工具链在 Linux CI 下对 CRLF 处理不一致
#   显式声明可保证「无论谁在什么系统 clone，检出结果完全一致」，消除噪音 diff。

* text=auto eol=lf

# 明确按文本处理并强制 LF
*.py      text eol=lf
*.pyi     text eol=lf
*.toml    text eol=lf
*.cfg     text eol=lf
*.ini     text eol=lf
*.json    text eol=lf
*.md      text eol=lf
*.txt     text eol=lf
*.yaml    text eol=lf
*.yml     text eol=lf
*.ts      text eol=lf
*.tsx     text eol=lf
*.js      text eol=lf
*.jsx     text eol=lf
*.css     text eol=lf
*.html    text eol=lf
*.sh      text eol=lf

# Windows 脚本必须保持 CRLF，否则 cmd/PowerShell 可能无法执行
*.bat     text eol=crlf
*.cmd     text eol=crlf
*.ps1     text eol=crlf

# 二进制 —— 禁止任何行尾或编码转换
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.webp    binary
*.ico     binary
*.pdf     binary
*.zip     binary
*.apk     binary
*.ttf     binary
*.otf     binary
*.woff    binary
*.woff2   binary

# 语言统计时排除生成物
packages/fortune-core/data/kangxi_strokes.json  linguist-generated=true
